# 练伴项目工作规则

本文件是练伴项目的强制工作规则。任何新的开发、修复、审查、验收、进度汇报或文档工作，**开始前必须先读取本文件**，再读取当前状态和与任务直接相关的证据。

## 开始工作前必须读取

按以下顺序执行：

1. `AGENTS.md`（本文件）；
2. `docs/product/lianban-v1.0-acceptance-ledger.md`（V1.0 总体状态）；
3. `docs/product/lianban-v1.0-work-log.md`（过程记录）；
4. 与当前任务直接相关的产品、UI、工程计划、合同、测试或交接文件。

若仓库存在更新的 `docs/product/lianban-v1-current-status.md`，将其放在第 2 步读取，并把验收台账作为第二份交叉核对资料。没有该文件时，不得自行假定项目状态；必须以 Git、台账和实际测试证据为准。

开始前还必须检查：

```powershell
git status --short
git log -10 --date=iso-local --pretty=format:'%h`t%ad`t%s'
```

工作树中的未提交改动必须视为当前施工内容，不得标记为稳定完成，也不得覆盖、还原或清理与当前任务无关的改动。

## 上下文和 token 使用规则

- 优先读取短的当前状态和台账，不重复加载整份历史交接或超长计划。
- 大文件必须先用 `rg -n` 按场景、状态、编号或错误码定向查找；只有命中相关段落时才读取局部范围。
- 每个任务只读取完成目标所需的最少文件；已收口场景不因惯性重复复核，除非相关代码、合同或环境发生变化。
- 将事实、历史和计划分开：当前状态看台账/状态摘要，过程看工作日志，详细证据看专项计划和测试。
- 测试按任务范围运行：日常优先 focused test；修改共享模块、提交前或独立复核前才运行全量回归。
- 不重复复制同一结果到多个文档；工作日志只追加增量记录，产品状态变化才同步更新验收台账。
- 提交信息必须包含场景或模块编号，例如 `feat(p11-15): ...`、`test(p11): ...`、`docs(p07): ...`。
- 汇报固定使用：`当前场景 / 已完成 / 本次证据 / 剩余阻断 / 下一步`，避免重复解释已冻结的完整背景。

## 证据和状态规则

- 文件存在、计划存在、路由可达或单项测试通过，都不能单独证明完整产品要求完成。
- 未执行的测试、浏览器操作、人工验收或外部门禁，必须明确写为未执行或未关闭。
- `test-only`、fixture、mock、隔离数据库和本地结构证据不得外推为生产、真人、G2 或 G3 通过。
- 测试失败必须保留原始错误、复现命令和当前结论；不得用重命名状态或删除测试隐藏失败。
- 认证、权限、数据库、迁移、隐私、专业规则、生产和真人数据属于高风险范围，必须先调查根因和边界，再提出修改。
- 练伴 V1.0 的产品范围以 `docs/product/lianban-v1.0-prd.md` 为唯一权威来源；历史交接、旧 API 合同和临时原型不能覆盖 PRD。
- UI、研发和产品保持职责边界：产品负责范围、规则、优先级和验收；UI 负责 `apps/web/**` 和 `docs/ui/**`；研发负责 `apps/api/**`、`packages/**` 和 `docs/engineering/**`。
- 演示内容必须保留“仅用于原型演示，未经专业审核”，不得进入真人发布；减脂和增肌两条路线必须分别验证，不按姓名或固定用户 ID 硬编码。

## 完成前检查

在声称完成、通过、修复或可交付前，必须运行与声明匹配的验证，并报告实际结果：

- 代码变更：相关 focused test、`npm run typecheck`、必要时 `npm run build`；
- 共享模块或提交前：`npm test`、`git diff --check`；
- 文档变更：内部链接、占位符、表格结构和 `git diff --check`；
- 浏览器/视觉要求：必须有实际运行、截图或交互证据，不接受只读 CSS 或代码推断。

如果验证失败，必须报告失败，不能使用“应该通过”“基本完成”替代证据。完成汇报还必须说明当前工作树和提交范围，确保没有误包含用户的其他改动。
