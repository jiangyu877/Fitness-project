# 练伴 V1.0 工作登记表

版本：0.1<br>
建立时间：2026-07-26<br>
维护范围：练伴 V1.0

## 使用说明

本表记录练伴 V1.0 施工过程中的重要问题、分析依据、实际修改、验收结果和后续动作。产品范围与业务规则以 [V1.0 PRD](./lianban-v1.0-prd.md) 为唯一权威来源，P01-P21 总体状态以 [产品验收台账](./lianban-v1.0-acceptance-ledger.md) 为准；本表只提供过程追踪，不替代审批或验收结论。

责任范围使用 `[产品]`、`[UI]`、`[研发]`、`[联合]` 标记。状态统一使用：

- `分析中`：正在收集证据，尚未形成可执行结论；
- `施工中`：已有修改或测试，但目标范围尚未完整验收；
- `待验收`：修改已完成，等待相应责任方复核；
- `已完成`：目标范围内已有匹配的修改和验收证据；
- `已阻断`：缺少必须由指定责任方提供的决定、批准或外部条件。

登记规则：未运行的测试不得写成通过；未提交改动不得写成稳定交付；路由存在、静态页面或单项测试不能单独证明完整产品要求通过；专业、安全、隐私、运营和部署门禁只能由对应责任方关闭。

## 登记表

| 时间 | 问题/目标 | 分析了什么 | 易错点 | 修改了什么 | 验收 | 状态/后续 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-23 | `[产品]` 冻结 V1.0 产品基线和交付范围 | 核对 [V1.0 PRD](./lianban-v1.0-prd.md) 的产品定位、明确排除项、P0/P1、角色、计划生命周期和 G0-G3 门槛；确认 V1.0 仅面向受邀健康成年人，目标路线仅减脂与增肌，完全排除 AI、公开注册、支付、媒体上传、医疗和康复能力。 | 历史产品资料、交接材料和实施附件不能覆盖 PRD；施工授权不等于真人封测批准。 | 建立并冻结 V1.0 PRD；建立 [P01-P21 验收台账](./lianban-v1.0-acceptance-ledger.md)。 | P01 在验收台账中为“通过”；其余要求仍按各自证据状态管理。 | `已完成`：继续以 PRD 为唯一范围基线；任何范围变化先走产品变更。 |
| 2026-07-23 | `[UI]` 冻结 32 个页面和双端 UI/UX 规格 | 核对 [UI/UX 设计规格](../ui/specs/2026-07-23-lianban-ui-ux-design.md) 的 H5/Web 信息架构、页面矩阵、目标视口和无障碍要求；应用测试包含 32 条冻结路由标题检查。 | “32 路由可达”不等于 32 个完整业务页面通过；通用状态页、桌面缩放预览或 CSS 检查不能替代六视口和真实浏览器验收。 | UI 规格维护 32 个唯一页面 ID；前端已建立路由和部分高保真/真实状态页面，当前仍有未提交 UI 施工改动。 | 2026-07-26 `npm test` 中 `renders the matching title for all 32 catalog paths` 通过；尚无 360/390/430、1024/1280/1440 六视口及 Narrator 完整证据。 | `施工中`：补齐页面业务深度、六视口截图/像素检查、键盘焦点、200% 缩放和 Narrator 验收。 |
| 2026-07-23 | `[联合]` 建立减脂/增肌双人物演示数据并隔离真人发布 | 核对 [演示人物资料](../ui/fixtures/2026-07-23-demo-personas.md)、UI 规格和 P03/P04；两条路线必须由目标类型与状态驱动，不得按姓名或固定用户 ID 硬编码；演示内容必须显示“仅用于原型演示，未经专业审核”。 | 演示数据真实感不能被解释为专业处方；隐藏 persona 切换器不能进入生产构建；仅有前端提示不能证明服务端和数据库不可发布。 | 已建立减脂新手与增肌新手 fixtures、受保护 demo API 和开发环境 persona 切换器；生产构建隔离脚本及相关前端改动当前尚未全部提交。 | 2026-07-26 根测试中 demo client、catalog 和本地 persona 切换测试通过；双路线真实 API 端到端闭环、发布绕过和生产包验收尚未全部完成。 | `施工中`：完成双路线 API/E2E、生产包检查及 API/UI/数据库跨层发布阻断证据。 |
| 2026-07-23 至 2026-07-26 | `[研发/UI]` 完成受邀身份、首次改密和会话恢复闭环 | 核对身份领域、API、数据库迁移、前端身份路由及 [身份恢复验收附件](./lianban-v1.0-identity-recovery-acceptance.md)；重点检查受限改密上下文、会话类型隔离、停用/过期状态、授权版本和审计。 | 受限改密令牌不能当作完整会话；客户端提供的角色、下一步或授权版本不能作为可信事实；本地 provider 不能被误写成生产认证方案。 | 已提交基础身份安全切片；当前工作树继续修改身份服务、控制器、恢复 E2E、迁移 008 和真实身份前端页面。 | 2026-07-26 根测试中身份客户端 14 项、身份路由 15 项及身份领域 15 项通过；身份恢复 E2E 有 2 项失败，其中 1 项超时、1 项 `ECONNRESET`。 | `施工中`：修复身份恢复 E2E，完成生产会话持久化、受治理 provider、MFA/密码/锁定/重置策略批准。 |
| 2026-07-23 至 2026-07-26 | `[研发/UI]` 实现计划双审核、发布、双确认、超时、生效和历史 | 核对 [计划生命周期验收附件](./lianban-v1.0-plan-lifecycle-acceptance.md)、领域状态机、计划仓储、迁移 009、HTTP 控制器和真实计划 UI；确认发布、用户确认和生效是三个独立节点。 | 待确认、退回、超时、等待生效和计划空档都不能生成今日任务；已发布版本不可原地修改；旧计划不得静默延期；客户端不得自行判定权限、超时或生效。 | 已提交领域状态机、内存 API 和初版持久化仓储；当前工作树继续修改持久化计划 HTTP、仓储事务、迁移 009 和真实计划 UI。 | 2026-07-26 根测试中计划领域 14 项和迁移 11 项通过；计划仓储 2 项失败，计划生命周期 E2E 至少 4 项失败，包含 `409 Conflict`、幂等重放和事务/重叠计划断言问题。 | `施工中`：先关闭仓储与 HTTP E2E 失败，再做减脂/增肌双路线、拒绝/超时/空档/并发/越权联合验收。 |
| 2026-07-23 至 2026-07-26 | `[研发]` 落实五角色、可信操作者、专业资格和自审禁止 | 核对 PRD 角色职责、[稳定技术契约](../engineering/contracts/phase-1-stable-contract.md)、计划授权和身份安全测试；运营不得批准专业内容或解除专业风险。 | UI 隐藏按钮不能替代服务端授权；多角色用户每次操作仍须记录当前角色；双重专业资格也不能审核自己直接编制的对应部分。 | 已提交身份授权拒绝审计、安全强化和计划领域职责分离；当前工作树继续调整统一 fail-closed 路由访问和相关 API 测试。 | 已提交的身份安全与拒绝审计测试有通过记录；2026-07-26 根测试未全绿，因此不能把 P06/P18 标记为总体通过。 | `施工中`：完成持久化计划 HTTP 越权测试、生产 MFA、审计查询及用户/员工生产会话隔离验收。 |
| 2026-07-23 至 2026-07-26 | `[联合]` 建立真人服务就绪门禁并保持默认阻断 | 核对 [真人服务就绪门禁](./lianban-v1.0-readiness-gates.md) 的八类阻断码、环境行为和证据责任；`readyForRealUsers=true` 只有在 demo 关闭且全部 G3 证据关闭时才允许出现。 | 自动化构造批准策略只验证允许分支，不是真实审批；单一专业签字不能清除其他门禁；未知阻断码或接口失败必须 fail closed。 | 已提交 readiness API、稳定阻断码和安全/隐私门禁；当前工作树继续调整路由访问快照与 fail-closed 行为。 | 2026-07-26 多项 readiness API 测试通过，但 OpenAPI 测试和健康检查各有 1 项 5 秒超时；G3 外部证据尚未齐全。 | `已阻断`：真人封测继续禁止；等待专业四领域、认证安全、隐私、数据权利、备份恢复、运营值班和部署安全证据。 |
| 2026-07-26 11:59 +08:00 | `[联合]` 建立当前仓库整体测试基线 | 运行根命令 `npm test`，覆盖领域、数据库、API 和 Web 的 Vitest 测试；结合当前 Git 状态区分已提交基线与大量未提交施工内容。 | 测试数量多不代表产品验收完成；并行测试超时可能掩盖真实缺陷，也不能在未复跑前归类为单纯性能问题；未提交施工状态随时可能变化。 | 本次只执行验证并建立登记表，没有修改测试或业务实现。 | `26` 个测试文件中 `21` 个通过、`5` 个失败；`266` 项测试中 `256` 项通过、`10` 项失败。失败覆盖计划仓储 2 项、身份恢复 2 项、OpenAPI 1 项、API 健康检查 1 项及计划生命周期 E2E 4 项。 | `施工中`：研发逐项修复并重新运行根测试；在完整回归全绿前不得宣称当前施工批次验收完成。 |
| 2026-07-26 | `[产品/联合]` 盘点尚未形成闭环的 V1.0 要求 | 对照验收台账 P07、P10-P13、P15-P17、P19-P21 以及 G2/G3；确认授权建档、每日记录、周反馈、风险处理、消息、数据权利、审计查询、无障碍、运维和四周指标仍缺完整证据。 | 不能因已有静态页面、字段骨架或局部自动化就把未开始项改为通过；专业字段、阈值、安全文案和调整边界不得由产品、UI 或研发自行补造。 | 本次未修改上述业务能力，只登记当前缺口。 | 当前验收台账将上述多数项目标记为“未开始”，G2 未达到，G3 禁止。 | `已阻断`：按验收台账施工顺序推进；涉及专业、安全、隐私、运营和部署的事项由对应责任方提交版本化证据。 |
| 2026-07-30 | `[产品/联合]` 同步 P11 当前交接、审查结论与 P11-06 | 对照 current API pre-wiring 计划、P11 产品附件、研发/UI 交接和用户指定 `D:` 工作树；Sol Critical 对 `record-version-conflict` 给出 `ALLOW_NEXT_SINGLE_SCENARIO`，产品仅选择精确重放。 | API fake 只证明 controller 映射；PG18 锁、并发、幂等和审计原子性仍独立归属 repository 证据。精确重放的 pre-wiring fake 场景直接 GREEN，不能伪造 RED 或把它解释为数据库无第二次写入。 | 激活唯一的精确重放测试；更新台账、P11 附件和总交接。未改动业务、API 实现、数据库或 UI。 | `npx vitest run` 的四个 P11 API 文件为 `32 passed / 20 skipped`；API typecheck 和 build 通过。本项仍需新的 Sol Critical 独立复审。 | `施工中`：复审前冻结其余 20 个 skipped；不接入数据库 adapter 或生产依赖，不开启真人/外部数据库/staging/部署/G2/G3。 |
| 2026-07-30 | `[产品/联合]` 收口 P11-06 独立复审 | 读取 Sol Critical 严格只读结论：无 Critical，工程计划计数滞后是已授权同步的非阻断审计性事项，无其他 Important 实现问题，结论 `ALLOW_NEXT_SINGLE_SCENARIO`。 | 结论只确认 test-only HTTP 映射切片；不证明 PostgreSQL 重放去重、锁、并发序列化或审计原子性，不构成生产、真人、G2/G3 或 release 批准。 | 研发部仅同步 API pre-wiring 计划的 9 active / 20 skipped、P11-06 完成与下一停点；产品部同步本行及当前停点。 | 计划文本与 active/skipped 结构已对齐；既有聚焦 API `32 passed / 20 skipped`、typecheck、build、diff/scope/staged-index 检查保持有效。 | `施工中/已停止`：P11-06 收口；其余 20 个 skipped 冻结，下一场景必须重新取得产品单场景授权并重新走 RED-GREEN-验证-独立复审。 |
| 2026-07-30 | `[产品]` 授权 P11-10 schema 版本冲突 | 依据 P11-06 的 Sol Critical `ALLOW_NEXT_SINGLE_SCENARIO` 和现行预接线计划，选择不涉及专业字段或生产依赖的 schema 版本冲突映射。 | 该场景只能证明 controller 对 scripted fake 的公开错误映射；不能证明数据库 schema 迁移、持久化冲突、锁、并发或审计原子性。 | 更新产品授权记录；未修改 API、数据库或 UI。 | 尚未激活测试，必须先出现预期 RED；其余 19 个 skipped 仍冻结。 | `施工中`：研发仅可开启该测试并执行 RED -> 最小 GREEN -> 聚焦验证；随后再次请求 Sol Critical 独立复审。 |
| 2026-07-30 | `[产品/联合]` P11-10 初步 GREEN 被独立复审阻断 | P11-10 已观察到 `400` 对 `409` 的 RED 并得到 `409 RECORD_SCHEMA_VERSION_CONFLICT` 初步 GREEN；Sol Critical 只读审查发现测试缺少 scripted fake 调用与旧 schema 输入的回归断言。 | 仅公开 409 断言无法证明请求实际到达 test-only port，未来实现可短路而不被测试发现。该问题不涉及 PostgreSQL、真人或生产，但阻断正式选择下一场景。 | 产品将台账和交接同步为 `10 active / 19 skipped`、当前 `BLOCK`；未改变授权范围。 | 已有四文件 `33 passed / 19 skipped`、typecheck/build 仅为初步 GREEN 证据，不能越过独立审查。 | `施工中/已阻断`：仅补足 P11-10 的 fake 调用和 `schema-v0` 输入断言，重跑聚焦验证并重新审查；其余 19 个 skipped 冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 P11-10 复审并授权 P11-07 | Sol Critical 第二次严格只读复审确认两项断言已闭合，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一项已有同键不同意图状态。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL 幂等持久化、重复写入抑制、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 10 active / 19 skipped，并仅授权 P11-07。 | 权威目录四文件 `33 passed / 19 skipped`、API typecheck/build、diff/scope/staged 检查通过；P11-07 尚未激活，必须先出现 RED。 | `施工中`：研发仅可开启 P11-07，完成 RED -> 最小 GREEN -> 聚焦验证；随后再次请求独立 Sol Critical，其他 18 个 skipped 冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 P11-07 复审并授权 RECORD_PLAN_NOT_ACTIVE | Sol Critical 严格只读复审确认 P11-07 的 fake 调用、原始幂等键和完整端口输入断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一项独立错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL 幂等持久化、重复写入抑制、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 11 active / 18 skipped，并仅授权 `RECORD_PLAN_NOT_ACTIVE`。 | 权威目录四文件 `34 passed / 18 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该错误映射，完成 RED -> 最小 GREEN -> 聚焦验证；随后再次请求独立 Sol Critical，其他 17 个 skipped 冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 RECORD_PLAN_NOT_ACTIVE 复审并授权 RECORD_STATE_BLOCKED | Sol Critical 严格只读复审确认 `RECORD_PLAN_NOT_ACTIVE` 的 fake 调用和完整端口输入断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个独立错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL 计划状态、持久化、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 12 active / 17 skipped，并仅授权 `RECORD_STATE_BLOCKED`。 | 权威目录四文件 `35 passed / 17 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该错误映射，完成 RED -> 最小 GREEN -> 聚焦验证；随后再次请求独立 Sol Critical，其他 16 个 skipped 冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 RECORD_STATE_BLOCKED 复审并授权 RECORD_SCHEMA_UNAVAILABLE | Sol Critical 严格只读复审确认 `RECORD_STATE_BLOCKED` 的 fake 调用和完整端口输入断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个 schema/provider 错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL 状态持久化、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 13 active / 16 skipped，并仅授权 `RECORD_SCHEMA_UNAVAILABLE`。 | 权威目录四文件 `36 passed / 16 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该错误映射，完成 RED -> 最小 GREEN -> 聚焦验证；随后再次请求独立 Sol Critical，其他 15 个 skipped 冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 RECORD_SCHEMA_UNAVAILABLE 复审并授权 RECORD_SCHEMA_INVALID | Sol Critical 严格只读复审确认 `RECORD_SCHEMA_UNAVAILABLE` 的 fake 调用和完整端口输入断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个 schema/provider 错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL schema/provider 持久化、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 14 active / 15 skipped，并仅授权 `RECORD_SCHEMA_INVALID`。 | 权威目录四文件 `37 passed / 15 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该错误映射，完成 RED -> 最小 GREEN -> 聚焦验证；随后再次请求独立 Sol Critical，其他 14 个 skipped 冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 RECORD_SCHEMA_INVALID 复审并授权 RECORD_REQUEST_INVALID | Sol Critical 严格只读复审确认 `RECORD_SCHEMA_INVALID` 的 fake 调用、完整端口输入和公开错误映射断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个独立请求错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL schema 有效性、持久化、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `38 passed / 14 skipped`，并仅授权 `RECORD_REQUEST_INVALID`；未修改 API、数据库或 UI。 | 权威目录四文件 `38 passed / 14 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该错误映射，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 13 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 RECORD_REQUEST_INVALID 复审并授权 ROUTE_ACCESS_NOT_APPROVED | Sol Critical 严格只读复审确认 `RECORD_REQUEST_INVALID` 的 fake 调用、完整端口输入和公开错误映射断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个 repository 路由阻断错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明 PostgreSQL 请求校验持久化、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `39 passed / 13 skipped`，并仅授权 `ROUTE_ACCESS_NOT_APPROVED`；未修改 API、数据库或 UI。 | 权威目录四文件 `39 passed / 13 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该错误映射，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 12 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 ROUTE_ACCESS_NOT_APPROVED 复审并授权 HMAC_KEY_UNAVAILABLE | Sol Critical 严格只读复审确认 `ROUTE_ACCESS_NOT_APPROVED` 的 endpoint fallback、fake 单次调用和完整端口输入断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个内部错误映射。 | 复审只确认 test-only controller 输入/输出映射，不证明全局路由、PostgreSQL route revalidation、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `40 passed / 12 skipped`，并仅授权内部 `HMAC_KEY_UNAVAILABLE`；未修改 API、数据库或 UI。 | 权威目录四文件 `40 passed / 12 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该内部错误映射，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 11 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 HMAC_KEY_UNAVAILABLE 复审并授权 trusted non-USER rejection | Sol Critical 严格只读复审确认内部 HMAC 错误的 endpoint fallback、fake 单次调用、完整端口输入和不泄露内部错误码断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个角色拒绝顺序场景。 | 复审只确认 test-only controller 映射与脱敏，不证明 HMAC key availability、PostgreSQL、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `41 passed / 11 skipped`，并仅授权 `trusted non-USER rejection ahead of the fake`；未修改 API、数据库或 UI。 | 权威目录四文件 `41 passed / 11 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启该角色拒绝场景，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 10 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 trusted NON_USER 复审并授权缺失 idempotency-key 前置拒绝 | Sol Critical 严格只读复审确认可信 NON_USER 角色校验顺序、403 公开 envelope 和 fake 零调用断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个请求头缺失场景。 | 复审只确认 test-only controller 授权顺序和公开映射，不证明生产身份策略、PostgreSQL、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `42 passed / 10 skipped`，并仅授权“缺失 idempotency-key 前置拒绝”；未修改 API、数据库或 UI。 | 权威目录四文件 `42 passed / 10 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启缺失 idempotency-key 场景，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 9 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口缺失 idempotency-key 复审并授权空白 idempotency-key 前置拒绝 | Sol Critical 严格只读复审确认缺失幂等键在 repository 输入构造前返回冻结 400 envelope、requestId 安全和 fake 零调用断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个独立空白头场景。 | 复审只确认 controller header fail-closed 映射，不证明生产认证策略、PostgreSQL、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `43 passed / 9 skipped`，并仅授权“空白 idempotency-key 前置拒绝”；未修改 API、数据库或 UI。 | 权威目录四文件 `43 passed / 9 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启空白 idempotency-key 场景，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 8 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口空白 idempotency-key 复审并授权缺失 x-request-id 安全 fallback | Sol Critical 严格只读复审确认空白幂等键在 repository 输入构造前返回冻结 400 envelope、fake 零调用和原始空白值不泄露断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个相关性 ID 缺失场景。 | 复审只确认 controller header fail-closed 映射，不证明生产认证策略、PostgreSQL、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `44 passed / 8 skipped`，并仅授权“缺失 x-request-id 安全 fallback”；未修改 API、数据库或 UI。 | 权威目录四文件 `44 passed / 8 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启缺失 x-request-id 场景，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 7 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口缺失 x-request-id 复审并授权空白 x-request-id 安全 fallback | Sol Critical 严格只读复审确认缺失相关性 ID时生成独立 UUID、在 fake 前返回冻结 400 envelope、requestId 不含敏感值且 fake 零调用断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个空白相关性 ID 场景。 | 复审只确认 controller correlation fallback/fail-closed 映射，不证明生产认证策略、PostgreSQL、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `45 passed / 7 skipped`，并仅授权“空白 x-request-id 安全 fallback”；未修改 API、数据库或 UI。 | 权威目录四文件 `45 passed / 7 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启空白 x-request-id 场景，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 6 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口空白 x-request-id 复审并授权 invalid command 前置拒绝 | Sol Critical 严格只读复审确认空白相关性 ID使用独立 UUID、在 fake 前返回冻结 400 envelope、fallback 不含敏感值且 fake 零调用断言，无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`；产品选择下一个命令校验场景。 | 复审只确认 controller correlation fallback/fail-closed 映射，不证明生产认证策略、PostgreSQL、锁、并发或审计原子性，不构成生产/真人/G2/G3/release 批准。 | 产品台账、总交接和 P11 附件同步为 `46 passed / 6 skipped`，并仅授权“invalid command 前置拒绝”；未修改 API、数据库或 UI。 | 权威目录四文件 `46 passed / 6 skipped`、API typecheck/build、diff/scope/staged 检查通过；下一场景尚未激活。 | `施工中`：研发仅可开启 invalid command 场景，先观察真实 RED，再做最小 GREEN、聚焦验证并重新请求独立 Sol Critical；其他 5 个场景继续冻结。 |
| 2026-07-30 | `[产品/联合]` 收口 invalid command 复审并授权缺失任务防枚举 404 | 研发完成真实控制器严格校验的聚焦验证：`400 RECORD_REQUEST_INVALID`、`CLEAR_ALL`、空动作、fake 零调用；四文件回归 `47 passed / 5 skipped`，API typecheck/build、diff/scope/staged 检查通过。Sol Critical 严格只读复审无 Critical/Important，结论 `ALLOW_NEXT_SINGLE_SCENARIO`。 | 该证据只证明 test-only controller 请求校验、公开映射和零 fake 调用，不证明持久化请求校验、PostgreSQL、防枚举查询、锁、并发或审计原子性；不构成生产、真人、G2/G3 或 release 批准。 | 产品台账、总交接和 P11 附件同步为 `47 passed / 5 skipped`；仅授权“缺失任务的防枚举 404”，未授权跨用户任务、会话/全局路由优先级、异常拒绝脱敏、数据库 adapter 或生产依赖注册。 | 本次仅同步产品文档，未新增 API、数据库或 UI 代码；工作树仍为共享未提交状态，暂存区为空。 | `施工中`：研发下一步只可激活缺失任务测试，先观察真实 RED，再做最小 GREEN、聚焦验证和独立 Sol Critical；其他 4 个 skipped 继续冻结。 |
| 2026-07-31 | `[产品/联合]` 同步缺失任务防枚举 404 证据并等待重新复审 | 研发完成单场景 RED、最小 GREEN 和四文件回归：真实 RED 为统一 fallback `503` 而非冻结 `404`，最小 GREEN 只映射精确 `RECORD_TASK_NOT_FOUND`；当前 `48 passed / 4 skipped`，API typecheck/build 已报告通过。专业部门初审代码/测试无 Critical/Important，但因工程计划过期返回 `BLOCK`；质量部因产品与工程文档漂移返回 `QA_BLOCK`，安全与隐私部返回 `SECURITY_NO_OBJECTION`。 | fake 只证明 controller 输入/输出、单次调用和端口输入；不得据此证明 PostgreSQL 查询、防枚举、锁、并发、幂等、审计原子性、生产或真人行为。 | 研发工程计划、产品台账、总交接和 P11 附件同步为 `25 active / 4 skipped`、`48 passed / 4 skipped`，并明确当前无下一场景授权；未修改 API、数据库或 UI。 | 只执行文档范围、diff 和 staged-index 检查；未运行测试、数据库或构建，沿用本批已报告的运行证据。 | `待验收`：请求专业部门重新严格只读 Sol Critical、质量部复核文档一致性；在二者解除阻断及产品二次授权前，cross-user、会话优先级、全局路由优先级和异常拒绝脱敏继续冻结。 |
| 2026-07-31 | `[产品/联合]` 收口缺失任务复审并授权跨用户防枚举 404 | 研发修复工程计划和受控合同的 test-only/production 边界矛盾后，专业部门最终 Sol Critical 与质量复核均无 Critical/Important，分别返回 `ALLOW_NEXT_SINGLE_SCENARIO`、`QA_CLEAR`；安全结论保持 `SECURITY_NO_OBJECTION`。 | 结论只确认 missing-task controller/fake 切片和文档一致性；不证明 PostgreSQL 查询、防枚举、锁、并发、幂等、审计原子性，不构成生产、真人、G2/G3 或 release 批准。 | 产品台账、总交接和 P11 附件同步最终结论，并仅授权“跨用户任务返回同一防枚举 404”；未授权其他三个 skipped、数据库 adapter 或生产依赖。 | 当前仍为已报告 `48 passed / 4 skipped`；下一场景尚未激活，暂存区保持为空。 | `施工中`：研发仅可激活跨用户任务场景，观察真实结果；直接 GREEN 时不得伪造 RED 或新增生产逻辑，完成聚焦验证和工程计划同步后重新请求专业 Sol Critical。 |
| 2026-07-31 | `[产品/联合]` 收口跨用户防枚举 404 并授权会话优先级 | cross-user 直接复用现有精确 `RECORD_TASK_NOT_FOUND` 映射 GREEN，无生产代码改动；四文件回归 `49 passed / 3 skipped`，API typecheck/build 通过。专业、安全、质量复核均无 Critical/Important，分别返回 `ALLOW_NEXT_SINGLE_SCENARIO`、`SECURITY_NO_OBJECTION`、`QA_CLEAR`。 | fake 只证明 controller 返回相同固定 404、单次调用和完整 `other-user-task` 输入，不证明 PostgreSQL 归属查询、防枚举、锁、并发、幂等或审计原子性。 | 产品文档同步为 `26 active / 3 skipped`、`49 passed / 3 skipped`，并仅授权“无效/过期会话优先于畸形输入”；全局路由优先级和异常拒绝脱敏继续冻结。 | 本次复核未重跑测试；运行结果来自研发本批已报告证据，暂存区为空。 | `施工中`：研发仅可激活会话优先级场景，观察真实 RED 或直接 GREEN，完成聚焦验证和工程计划同步后重新请求专业 Sol Critical。 |
| 2026-07-31 | `[产品/联合]` 收口会话优先级并授权全局路由优先级 | INVALID/EXPIRED 场景真实 RED 为 `401 SESSION_INVALID` 携带未授权 `LOGIN`；最小 GREEN 仅移除该动作，四文件回归 `50 passed / 2 skipped`，API typecheck/build 通过。专业、安全、质量复核均无 Critical/Important，分别返回 `ALLOW_NEXT_SINGLE_SCENARIO`、`SECURITY_NO_OBJECTION`、`QA_CLEAR`。 | 证据只证明 P11 controller 的身份拒绝顺序、可信 fallback、非泄露和 fake 零调用；不证明生产身份策略、PostgreSQL、锁、并发、幂等或审计原子性。 | 产品文档同步为 `27 active / 2 skipped`、`50 passed / 2 skipped`，并仅授权“全局路由 BLOCK 优先于无效会话和畸形请求”；异常 fake 拒绝脱敏继续冻结。 | 本次复核未重跑测试；运行结果来自研发本批已报告证据，暂存区为空。 | `施工中`：研发仅可激活全局 route-first 场景，观察真实 RED 或直接 GREEN，完成聚焦验证和工程计划同步后重新请求专业 Sol Critical。 |
| 2026-07-31 | `[产品/联合]` 收口全局 route-first 并授权异常拒绝脱敏 | 全局 BLOCK 场景 direct GREEN；指定 Sol Critical 初审发现 `invalid-task` 未真正进入请求，研发补齐第三参数并重跑后，复审无 Critical/Important、结论 `ALLOW_NEXT_SINGLE_SCENARIO`。四文件回归 `51 passed / 1 skipped`，API typecheck/build 通过。 | 证据只证明测试注入下全局 middleware 顺序、固定 503 envelope、非泄露和 fake 零调用；不证明生产路由策略、PostgreSQL、锁、并发、幂等或审计原子性。 | 产品文档同步为 `28 active / 1 skipped`、`51 passed / 1 skipped`，并仅授权最后一个“unexpected fake rejection 脱敏”场景。 | 指定 Sol Critical 未重跑测试；运行结果来自研发本批已报告证据，暂存区为空。 | `施工中`：研发仅可激活最后一个异常拒绝场景，观察真实 RED 或直接 GREEN，完成聚焦验证和工程计划同步后交同一指定 Sol Critical 复审。 |
| 2026-07-31 | `[产品/联合]` 收口全部 API pre-wiring 场景 | 最后的 unexpected fake rejection 场景 direct GREEN，无生产代码改动；四文件回归 `52 passed / 0 skipped`，API typecheck/build 通过。唯一指定 Sol Critical 复审无 Critical/Important，结论 `API_PREWIRING_REVIEW_COMPLETE`。 | 证据只证明 controller 的统一异常映射、固定 503 envelope、fake 单次调用及异常/秘密不回显；不证明 HMAC、PostgreSQL、锁、并发、幂等、审计原子性、生产或真人行为。 | 产品台账、总交接和 P11 附件同步为 `29 active / 0 skipped`、`52 passed / 0 skipped`；未授权 database adapter、production registration、真实 schema、UI/真人成功路径或下一阶段。 | 指定 Sol Critical 未重跑测试；运行结果来自研发本批已报告证据。暂存区为空，未提交、推送或部署。 | `已完成/已停止`：当前 API pre-wiring 阶段收口；任何下一阶段必须重新冻结产品范围并取得用户授权，G2 未达到，G3 与 `readyForRealUsers=true` 继续禁止。 |
| 2026-07-31 | `[产品/研发/质量]` 完成 test-only GET_RECORD_CONTEXT 成功预接线 | 项目发起人批准单场景设计；研发先观察期望 200、实际 `404 RECORD_TASK_NOT_FOUND` 的真实 RED，再建立独立 context read port/token、双重 test-only 注入、严格七字段响应和严格 OpenAPI。 | fake 只证明 controller 映射、调用、脱敏和 test-only 门禁；非 test 结构测试可能先被 readiness 503 拦截，只能证明 reader 未注入、fake 零调用和无成功响应，不能证明生产可信 USER 运行时 404，更不能证明 PostgreSQL 查询、归属、防枚举、锁、并发、幂等或审计。 | 新增 GET context 测试与独立 read port/token，最小修改 controller/application/AppModule/test builder/OpenAPI；未修改数据库、UI、专业字段或生产注册。产品设计、台账、P11 附件和总交接同步当前证据。 | context `1 passed`、OpenAPI `4 passed`、五文件 `53 passed / 0 skipped`，API typecheck/build 通过。QA 无 Critical/Important，`QA_CLEAR`；唯一指定 Sol Critical 无 Critical/Important，结论 `GET_CONTEXT_PREWIRING_REVIEW_COMPLETE`。 | `已完成/已停止`：仅收口 test-only GET 成功预接线。数据库 read adapter、UI runtime consumer、生产、真人、G2/G3、release 和下一阶段均未授权。 |
| 2026-08-03 | `[产品/UI/质量/安全]` 完成 test-only GET_RECORD_CONTEXT UI consumer | 项目发起人批准单一 UI consumer slice；UI 复用严格 parser/client-state，先观察 client/page/非 test gate RED，再最小 GREEN；修复 `SESSION_INVALID` 动作契约为当前 API 的 `[]`，同步 UI/产品/总交接文档。 | UI 证据只证明 fake/test transport、严格 envelope 解析、trusted USER session/opaque taskId、错误 fail closed、非 test 不请求且不回退 demo；不证明 API persistence、PostgreSQL、浏览器/无障碍、真人、G2/G3 或 release。 | 新增 `P11Client`、`P11RecordPage`、非 test route gate 及测试；未修改 API、数据库或专业字段。 | 非页面 `66/66`、页面 `6/6`、Web typecheck 通过；QA `QA_CLEAR`；指定 Sol Critical `UI_CONTEXT_CONSUMER_REVIEW_COMPLETE`，无 Critical/Important；暂存区为空，未提交、推送或部署。 | `施工中/已停止`：本 UI test-only slice 收口；不授予下一场景。数据库 read adapter、生产依赖、浏览器真人验收、G2/G3 和 readyForRealUsers 继续禁止。 |
| 2026-08-04 | `[产品/研发]` 完成 test-only PostgreSQL context read adapter 单场景并收口复审 | 项目发起人批准一个且仅一个 P11 数据库读取场景；先在真实隔离 PostgreSQL 18 上观察 `repository.getContext is not a function` RED，再新增独立 context repository。指定 Sol Critical 初审指出 ACTIVE 无限窗口、provider/schema 未绑定和 generic projection 未严格校验；研发在同一切片内新增对应 RED 并最小 GREEN，adapter 现在使用可信当前时间要求有限 `effectiveTo`、要求 test-only provider schema 与 gate/task 同版本，并按 kind/field/required/valueType 校验持久化记录。 | 修复后 adapter 聚焦 `4 passed`；完整 database workspace 回归 `138 passed`；database typecheck/build 通过。指定 Sol Critical 最终结论为 `P11_CONTEXT_ADAPTER_REVIEW_COMPLETE`，仅限当前单场景，无 Critical/Important。证据仍不覆盖跨用户防枚举、并发/锁、幂等、审计原子性、API/生产注册或真人行为。 | 仅新增 `packages/database/src/record-context-repository.ts`、数据库包导出和 `packages/database/test/record-context-repository.spec.ts`；未修改 API、UI、migration、专业字段或生产依赖。产品台账、P11 附件和总交接同步当前范围与证据边界。 | 当前工作树仍共享未提交，暂存区为空；未部署、未连接外部/生产数据库。API context port 尚未传入 schema，任何 API 接入必须另行设计授权。 | `已完成/已停止`：当前 adapter 单场景收口；不得据此接 API、注册生产依赖、进入真人路径、G2/G3 或开启下一场景。 |
| 2026-08-04 | `[产品/研发/安全]` 收口 test-only API context wiring | 研发按单场景 TDD 先复现 fake 收到 `undefined` schema 的 RED，再最小修改端口签名和 controller 调用，将已通过 test-only gate 的 `P11RecordPortSchema` 传入 context reader，并保留同一 schema 的响应映射；未新增 HTTP 场景、数据库接入或生产注册。 | 聚焦 context `1 passed`；五文件 P11 API 回归 `53 passed / 0 skipped`；API typecheck/build 通过。指定 Sol Critical 只读复审无 Critical/Important，确认 port 必填 schema、双 test-only gate、production token null/zero calls 和无成功响应。fake/controller 证据不覆盖 PostgreSQL、归属、防枚举、锁、并发、幂等、审计、生产、真人或 G2/G3。 | 仅修改 `apps/api/src/records/p11-record-context.port.ts` 与 `apps/api/src/records/record-safe-structure.controller.ts`；产品同步台账、P11 附件和总交接；未修改专业字段、UI、数据库或部署。 | 工作树共享未提交，暂存区为空；未提交、推送、部署或连接外部/生产数据库。 | `已完成/已停止`：本 wiring slice 收口；不得自动开启下一场景。保持 `readyForRealUsers=false`、G2 未达到、G3 禁止，下一步需用户重新授权产品单场景。 |
| 2026-08-04 | `[产品/研发/质量/安全/专业/UI/运营]` 评估下一候选 API-to-PostgreSQL context adapter 接入 | 产品提出下一候选仅限 test-only API 接入已审查的 PG18 context adapter；研发核查确认 API `DatabaseService` 使用 PGlite，而 adapter 要求 `pg.Pool`、独立 session/task/plan/gate/record 数据，不能直接注入或把 PGlite 强转为 Pool。质量、安全、专业、UI 分别完成只读边界核对；运营对未完成依赖授权保持 BLOCK；指定 Sol Critical 未提前放行。 | 当前未修改代码、未运行数据库、未开启 skipped。若继续，必须另行批准 test-only `pg` 运行时依赖、隔离 PG18 Pool 生命周期、PGlite/API 与 PG18 双数据库 fixture、关闭顺序和证据分层；不得生产注册、连接外部/生产数据库、进入真人路径或改变 G2/G3 门禁。 | 产品只记录阻断与授权条件，研发暂不施工；UI、专业和运营无代码动作。 | 工作树共享未提交，暂存区为空；本次未部署、未提交、未推送。 | `已阻断/待授权`：在用户明确批准上述 test-only 双数据库与 Pool 依赖边界前，不得开始 RED/GREEN 或请求 Sol Critical 放行。 |
| 2026-08-04 | `[产品]` 批准 test-only API-to-PostgreSQL context adapter 单场景 | 用户明确批准新增 test-only `pg` 运行时依赖、隔离 PG18 Pool 生命周期以及 PGlite/API 与 PG18 双数据库 fixture；仍禁止生产注册、外部/生产数据库、真人路径、部署、G2/G3 和 `readyForRealUsers` 放行。 | 该授权只覆盖一个 API adapter 接入场景；必须由研发执行 RED -> 最小 GREEN -> 聚焦验证，QA 核对后交唯一指定 Sol Critical 独立复审。 | 产品冻结验收口径和停止条件；研发获得施工授权，其他部门按各自边界协同。 | 工作树保持共享未提交，禁止暂存、提交、推送。 | `施工中`：只允许本单场景；任何第二场景或生产依赖必须重新取得产品授权。 |
| 2026-08-04 | `[产品/研发/质量/安全]` 收口 test-only API-to-PostgreSQL context adapter 单场景 | 研发先观察旧路径 `404 RECORD_TASK_NOT_FOUND` 的有效 RED，再由 test-only builder 创建隔离 PG18 Pool/P11RecordContextRepository，保留 API PGlite 身份库与 PG18 context 库双 fixture；QA 发现并推动修复 cleanup 错误聚合与 schema 双重强转两个 Important。 | 修复后 PG18 单文件 `3 passed`；六文件 P11 回归 `56 passed / 0 skipped`；API typecheck/build/diff/staged 通过。QA `QA_CLEAR`；指定 Sol Critical `P11_CONTEXT_ADAPTER_REVIEW_COMPLETE`，GREEN/ALLOW，无 Critical/Important。证据仅覆盖 test-only adapter 接入、双库 fixture、Pool 生命周期、schema fail-closed 和既有 controller 映射，不覆盖生产、真人、G2/G3 或 release。 | 仅修改研发测试 builder、PG18 API 集成测试、API 测试依赖和工程计划；未注册生产 adapter、未修改 UI、专业字段或数据库生产路径。 | 工作树共享未提交，暂存区为空；未提交、推送、部署或连接生产数据库。 | `已完成/已停止`：本单场景收口；不得自动开启下一场景。保持 `readyForRealUsers=false`、G2 未达到、G3 禁止；下一步需重新由产品冻结范围并取得用户授权。 |
| 2026-08-04 | `[产品]` 授权 P11-11 关闭 context 只读读取单场景 | 依据 P11 安全结构附件第 7 节及工程契约 P11-11，选择不引入专业语义的既有记录只读分支；要求服务端关闭事实、可信 USER/本人任务、schema/版本绑定和读取零副作用。 | `READ_ONLY` 读取不等于允许补录；不得把关闭时点、补录时限、专业字段或客户端状态推导成新规则。PG18 证据只限本场景，不能扩大为生产并发/审计闭环。 | 产品冻结单一场景、允许/禁止范围、固定 RED-GREEN-QA-Sol Critical-收口流程；未修改 API、数据库或 UI。 | 尚未运行 RED；研发仅可在 test-only 双库 fixture 中施工，QA 后交唯一指定 Sol Critical 复审。 | `施工中`：其余场景冻结；发现需要第二 HTTP 行为、生产依赖、专业批准、UI 或真人路径时立即停止。 |
| 2026-08-04 | `[产品/研发/质量/Sol Critical]` 收口 P11-11 关闭 context 只读读取 | 研发在同一授权 slice 内先观察 `READ_ONLY` 携带写动作和部分快照的阻断，再最小修复为关闭状态空 `allowedActions` 与七张实际迁移表 `SELECT *` 完整行/计数前后比较；服务端 date_state、USER/任务归属和真实 controller/PG18 adapter 保持不变。 | `READ_ONLY` 不能携带 `UPSERT_RECORD`；局部列投影不能宣称完整零副作用。PG18 读取证据仍不等同于生产锁、并发、幂等或审计原子性。 | controller 仅增加 READ_ONLY 空动作映射；PG18 测试补齐完整七表快照；工程计划记录真实 RED/GREEN、修复和证据边界。产品未修改专业字段、UI、生产路径或部署。 | P11-11 `1 passed`（该文件其余 3 skipped）；PG18 `4 passed`；六文件串行 `57 passed / 0 skipped`；API typecheck/build、diff/check、staged-index 通过。QA `QA_CLEAR`；指定 Sol Critical `GREEN / ALLOW — P11-11_REVIEW_COMPLETE`，Critical 0、Important 0。 | `已完成/已停止`：仅收口本单场景。`readyForRealUsers=false`、G2 未达到、G3 禁止；跨用户负例、并发/锁、持久化幂等/审计原子性、生产注册、真人和下一场景继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-12 已关闭日期既有记录 UPSERT 拒绝单场景 | 依据 P11 安全结构附件第 7 节、P11-12 与现有 PG18 repository 的服务端状态锁定能力，选择一个不引入专业语义的关闭日期写拒绝分支。 | 不得把该场景扩成补录时限或所有关闭分支；API fake、PG18 repository 与跨层 test-only adapter 证据必须分层陈述。 | 产品冻结 `date_state=CLOSED` + 本人既有记录 + 一次 `UPSERT_RECORD` 的精确范围，要求稳定 `RECORD_STATE_BLOCKED` 和七表完整零副作用；未修改 API、数据库或 UI。 | 尚未运行 RED；研发为唯一写入者，QA/安全/专业只读，最终只交指定 Sol Critical。 | `施工中`：其他 P11-12 分支与所有后续场景冻结；若需要新专业规则、生产依赖或外部数据库立即停止。 |
| 2026-08-16 | `[产品/研发/质量/安全/Sol Critical]` 收口 P11-12 已关闭日期既有记录 UPSERT 拒绝 | 研发先排除 PG18 未启动和默认超时等无效 RED；有效 RED 为 test builder 未接 PG18 write pool 时 controller 返回 `503`。最小 GREEN 仅增加 test-only `recordRepositoryPool` 与受控异常到 API port outcome 的适配器。 | 基础设施失败不得冒充行为 RED；工程计划必须同时登记 context 200 与 P11-12 409 两项新增 HTTP 行为。该证据不证明其他关闭分支、生产事务或通用原子性。 | 新增一个真实 API-to-PG18 CLOSED 日期合法 UPSERT 拒绝测试及七表完整快照；按 Sol Critical finding 同步工程计划 spec coverage/scope check；未修改生产注册、database package、UI 或专业字段。 | P11-12 `1 passed`，PG18 文件 `5 passed`，六文件串行 `58 passed / 0 skipped`；API typecheck/build、diff/check、staged-index 通过。QA `QA_CLEAR`，安全 `SECURITY_NO_OBJECTION`；指定 Sol Critical `GREEN / ALLOW — P11-12_REVIEW_COMPLETE`，Critical 0、Important 0。临时数据库已删除，本地 PG18 恢复原停止状态。 | `已完成/已停止`：仅收口本分支。其他关闭/补录分支、下一场景、生产、真人、G2/G3 和 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-12 任务已关闭既有记录 UPSERT 拒绝单场景 | 继关闭日期分支收口后，只选择服务端 `task_state=CLOSED` 分支，复用同一真实 API-to-PG18、可信 USER、已有记录和零副作用验收结构。 | 不得把直接 GREEN 伪造成 RED；不得扩成风险阻断、创建/补录或关闭时限规则。 | 产品冻结精确分支与固定 409 信封、七表完整快照、部门流程；未修改 API、数据库或 UI。 | 尚无本分支执行证据；研发为唯一写入者。 | `施工中`：仅允许该单场景；其余 P11-12 与所有后续场景冻结。 |
| 2026-08-16 | `[产品/研发/质量/安全/Sol Critical]` 收口 P11-12 任务已关闭既有记录 UPSERT 拒绝 | 复用已审查 test-only write adapter，新增仅把服务端 `task_state` 设为 `CLOSED` 的真实 API-to-PG18 场景；首次聚焦即 direct GREEN，未伪造 RED。 | 任务关闭与日期关闭必须分场景记录；direct GREEN 不授权顺带修改运行时代码或扩展风险/补录语义。 | 仅新增 PG18 场景并同步工程计划授权范围、direct GREEN、计数与停止条件；未修改 controller、adapter、database package、UI 或生产注册。 | PG18 文件 `6 passed`，六文件串行 `59 passed / 0 skipped`；API typecheck/build、diff/check、staged-index 通过。QA `QA_CLEAR`，安全 `SECURITY_NO_OBJECTION`；指定 Sol Critical `GREEN / ALLOW — P11-12_TASK_STATE_CLOSED_REVIEW_COMPLETE`，Critical 0、Important 0。临时库为空，PG18 恢复原停止状态。 | `已完成/已停止`：仅收口 task-state 分支。风险阻断、其他关闭/补录分支、后续场景、生产、真人、G2/G3 继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-12 风险阻断既有记录 UPSERT 拒绝单场景 | 继任务关闭分支收口后，只选择服务端 `risk_state=BLOCKED` 分支，复用同一真实 API-to-PG18、可信 USER、本人已有记录和七表零副作用验收结构。 | 该分支只验证既有机器状态；不得定义风险阈值、触发条件、影响范围、恢复条件、安全文案或 SLA，不得扩成第二风险/关闭/补录场景。 | 产品冻结精确分支、固定 409 信封、七表完整快照、部门分工和停止条件；未修改 API、数据库或 UI。 | 尚无本分支执行证据；研发为唯一代码写入者，QA/安全/专业/UI/运营只读，最终只交指定 Sol Critical。 | `施工中`：仅允许本单场景；发现需新增专业语义、生产依赖、外部数据库、UI 改动或真人路径时立即停止。 |
| 2026-08-16 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-12 风险阻断既有记录 UPSERT 拒绝 | 新增一个真实 API-to-PG18 场景，仅把服务端 `risk_state` 设为 `BLOCKED`，保持 task/date OPEN；首次聚焦 direct GREEN，未伪造 RED。 | 既有机器状态覆盖不能解释为风险规则、生产原子性或发布就绪；本分支复审完成也不自动授权下一场景。 | 仅新增 PG18 测试并同步工程计划；未修改本轮 controller、adapter、repository、UI 或生产注册。产品只同步收口证据。 | 研发 PG18 `7 passed`、六文件 `60 passed / 0 skipped`；QA 独立复现聚焦 `1 passed / 6 skipped`、PG18 `7 passed`、六文件 `60 passed / 0 skipped`，typecheck/build/diff/staged 通过，临时库 0，PG18 恢复停止。安全/专业/UI/运营无阻断；指定 Sol Critical `GREEN / ALLOW — P11-12_RISK_STATE_BLOCKED_REVIEW_COMPLETE`，Critical/Important/Minor 0。 | `已完成/已停止`：仅收口本 risk-state 分支。其他风险/关闭/补录、后续场景、生产、真人、G2/G3、release 与 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-12 风险阻断创建拒绝单场景 | 现有风险阻断测试覆盖“已有记录修改”，P11-12 仍需补齐“无记录创建”拒绝；选择服务端 `risk_state=BLOCKED`、可信 USER/本人 task、无已有记录、`expectedRecordVersion=null` 的一次合法 `UPSERT_RECORD`。 | 该场景只补齐创建拒绝覆盖，不得定义风险规则或扩成其他风险/关闭/补录语义；完整七表零副作用仍是必要验收。 | 产品冻结精确输入、409 信封、七表快照、部门分工和停止条件；未修改 API、数据库或 UI。 | 尚无本分支执行证据；研发为唯一代码写入者，QA/安全/专业/UI/运营只读，最终只交指定 Sol Critical。 | `施工中`：仅允许本单场景；发现需新增专业语义、生产依赖、外部数据库、UI 改动或真人路径时立即停止。 |
| 2026-08-16 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-12 风险阻断创建拒绝 | 新增一个真实 API-to-PG18 场景，不插入本人 record，只把服务端 `risk_state` 设为 `BLOCKED`，保持 task/date OPEN，并使用 `expectedRecordVersion=null`；首次有效聚焦 direct GREEN。 | 无记录创建拒绝不能外推为风险规则、生产原子性或发布就绪；本分支完成不自动授权下一场景。 | 仅新增 PG18 测试并同步工程计划；未修改本轮 controller、builder、repository、database package、UI 或生产注册。产品只同步收口证据。 | 研发 PG18 `8 passed`、六文件 `61 passed / 0 skipped`；QA 独立复现聚焦 `1 passed / 7 skipped`、PG18 `8 passed`、六文件 `61 passed / 0 skipped`，typecheck/build/diff/staged 通过，临时库 0，PG18 恢复停止。安全/专业/UI/运营无阻断；指定 Sol Critical `GREEN / ALLOW — P11-12_RISK_STATE_BLOCKED_FIRST_WRITE_REVIEW_COMPLETE`，Critical/Important/Minor 0。 | `已完成/已停止`：仅收口本 first-write 分支。其他风险/关闭/补录、后续场景、生产、真人、G2/G3、release 与 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-12 关闭日期创建拒绝单场景 | P11-12 已有关闭日期“既有记录修改拒绝”，但尚缺“无记录创建拒绝”；选择服务端 `date_state=CLOSED`、`task_state=OPEN`、`risk_state=CLEAR`、可信 USER/本人 task、无本人 record、`expectedRecordVersion=null` 的一次合法 `UPSERT_RECORD`。 | 该场景只补齐关闭日期创建拒绝，不得定义关闭时点、补录时限、专业字段或补录语义；API fake、PG18 repository 和跨层 test-only 证据必须分层。 | 产品冻结精确输入、固定 409 信封、七表完整行/计数零副作用、部门分工和停止条件；未修改 API、数据库或 UI。 | 尚无本分支执行证据；研发为唯一代码写入者，QA/安全/专业/UI/运营只读，最终只交指定 Sol Critical。 | `施工中`：仅允许本单场景；发现需新增机器契约、专业规则、生产依赖、外部数据库、UI 改动或真人路径时立即停止。 |
| 2026-08-16 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-12 关闭日期创建拒绝 | 新增一个真实 API-to-PG18 场景，不插入本人 record，只将服务端 `date_state` 设为 `CLOSED`，保持 task OPEN、risk CLEAR，并使用 `expectedRecordVersion=null`；首次有效聚焦 direct GREEN。 | 无记录创建拒绝不能外推为关闭时点、补录时限、生产原子性或发布就绪；本分支完成不自动授权下一场景。 | 仅新增 PG18 测试并同步工程计划；未修改本轮 controller、builder、repository、database package、UI 或生产注册。产品同步最终收口证据。 | 研发 PG18 `9 passed`、六文件 `62 passed / 0 skipped`；QA 独立复现聚焦 `1 passed / 8 skipped`、PG18 `9 passed`、六文件 `62 passed / 0 skipped`，typecheck/build/diff/staged 通过，临时库 0，PG18 停止且 5432 无监听。安全/专业/UI/运营无阻断；指定 Sol Critical `GREEN / ALLOW — P11-12_DATE_STATE_CLOSED_FIRST_WRITE_REVIEW_COMPLETE`，Critical/Important/Minor 0。 | `已完成/已停止`：仅收口本 date-state first-write 分支。其他关闭/风险/补录、后续场景、生产、真人、G2/G3、release 与 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-12 任务关闭创建拒绝单场景 | P11-12 已有任务关闭“既有记录修改拒绝”，但尚缺“无记录创建拒绝”；选择服务端 `task_state=CLOSED`、`date_state=OPEN`、`risk_state=CLEAR`、可信 USER/本人 task、无本人 record、`expectedRecordVersion=null` 的一次合法 `UPSERT_RECORD`。 | 该场景只补齐任务关闭创建拒绝，不得定义任务关闭条件、关闭时点、补录时限、专业字段或补录语义；API fake、PG18 repository 和跨层 test-only 证据必须分层。 | 产品冻结精确输入、固定 409 信封、七表完整行/计数零副作用、部门分工和停止条件；未修改 API、数据库或 UI。 | 尚无本分支执行证据；研发为唯一代码写入者，QA/安全/专业/UI/运营只读，最终只交指定 Sol Critical。 | `施工中`：仅允许本单场景；发现需新增机器契约、专业规则、生产依赖、外部数据库、UI 改动或真人路径时立即停止。 |
| 2026-08-16 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-12 任务关闭创建拒绝 | 新增一个真实 API-to-PG18 场景，不插入本人 record，只将服务端 `task_state` 设为 `CLOSED`，保持 date OPEN、risk CLEAR，并使用 `expectedRecordVersion=null`；首次有效聚焦 direct GREEN。 | 无记录创建拒绝不能外推为任务关闭条件、关闭时点、补录时限、生产原子性或发布就绪；本分支完成不自动授权下一场景。 | 仅新增 PG18 测试并同步工程计划；未修改本轮 controller、builder、repository、database package、UI 或生产注册。产品同步最终收口证据。 | 研发 PG18 `10 passed`、六文件 `63 passed / 0 skipped`；QA 独立复现聚焦 `1 passed / 9 skipped`、PG18 `10 passed`、六文件 `63 passed / 0 skipped`，typecheck/build/diff/staged 通过，临时库 0，PG18 停止且 5432 无监听。安全/专业/UI/运营无阻断；指定 Sol Critical `GREEN / ALLOW — P11-12_TASK_STATE_CLOSED_FIRST_WRITE_REVIEW_COMPLETE`，Critical/Important/Minor 0。 | `已完成/已停止`：仅收口本 task-state first-write 分支。其他关闭/风险/补录、后续场景、生产、真人、G2/G3、release 与 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-16 | `[产品]` 授权 P11-03 跨用户任务防枚举单场景 | P11 仍缺真实 API-to-PG18 的跨用户归属负例；选择可信 USER A 请求归属于完整虚构 USER B account/plan/plan_version/task 的一次合法 `UPSERT_RECORD`。 | API fake、PG18 repository 单测和跨层测试证据必须分层；不得把固定 404 与七表零副作用外推为生产防枚举、真人安全或通用事务原子性。 | 产品冻结 USER A/B 纯虚构归属、固定 `404 RECORD_TASK_NOT_FOUND / CLEAR_ALL / []`、七表完整零副作用、部门分工和停止条件；未修改 API、数据库或 UI。 | 尚无本分支执行证据；研发为唯一代码写入者，QA/安全/专业/UI/运营只读，最终只交指定 Sol Critical。 | `施工中`：仅允许本单场景；若需要修改运行时实现、增加第二场景、连接外部数据库或引入专业/真人/部署范围，立即停止。 |
| 2026-08-16 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-03 跨用户任务防枚举 | 新增真实 API-to-PG18 场景：USER A 有效会话请求完整一致的虚构 USER B account/plan/plan_version/task；首次聚焦 direct GREEN。 | 固定 404 与七表零副作用只证明本地跨层 fixture；API fake、repository 单测和跨层证据不得混同或外推为生产防枚举、真人安全或通用事务原子性。 | 仅新增 PG18 测试并同步工程计划；响应排除 USER B、plan/version/task、schema 和状态 sentinel；未修改 controller、builder、repository、database package、UI 或生产注册。 | 研发 PG18 `11 passed`、六文件 `64 passed / 0 skipped`；QA 独立复现聚焦 `1 passed / 10 skipped`、PG18 `11 passed`、六文件 `64 passed / 0 skipped`，typecheck/build/diff/staged 通过，临时库 0，PG18 停止且 5432 无监听。安全/专业/UI/运营无阻断；指定 Sol Critical `GREEN / ALLOW — P11-03_CROSS_USER_NON_ENUMERATION_REVIEW_COMPLETE`，Critical/Important/Minor 0。 | `已完成/已停止`：仅收口本 P11-03 cross-user 分支。其他场景、生产、真人、G2/G3、release 与 `readyForRealUsers=true` 继续冻结。 |

| 2026-08-18 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-03 不存在任务真实 API-to-PG18 单场景 | 在已收口跨用户防枚举之后，选择同一 P11-03 的不存在任务负例：可信 USER 请求无 `record_task` 行的 opaque 任务标识并提交合法 `UPSERT_RECORD`。 | 不得把缺失任务与跨用户结果混淆；必须保持固定 404、最小披露、7 张 authority + 3 张副作用共 10 张实际迁移表完整行/count 前后一致；不得批量开启 skipped、修改生产 wiring 或引入专业语义。 | 研发仅补齐 test-only 快照证据，未修改生产 controller、repository、database package、UI 或生产 wiring；产品同步本台账、总交接、本附件和工程执行记录。 | 研发首次聚焦 direct GREEN；真实 API→PG18 返回 `404 RECORD_TASK_NOT_FOUND / CLEAR_ALL / []`，响应不回显 token/task/schema/idempotency/entry/fixture sentinel；7 张 authority 表与 3 张副作用表完整 rows/count 前后相等。研发与 QA 聚焦 `1 passed / 11 skipped`、PG18 文件 `12 passed / 0 skipped`，typecheck/diff 通过，临时 PG18 已清理并停止、5432 无监听。指定 Sol Critical 重新复审为 `GREEN / ALLOW — P11-03_MISSING_TASK_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0。证据仅限 test-only API→PG18，不外推生产、真人、并发/锁、持久化幂等/审计原子性、G2/G3 或 release。 | `已完成/已停止`：仅收口本场景；下一场景须另行产品冻结并重新走单场景流程，其他 skipped、生产、真人、G2/G3、release 与 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-18 | `[产品]` 授权 P11-04-PLAN_NOT_ACTIVE 单场景 | P11-04 只取一个已有机器状态分支：服务端确定 task 关联的 plan version 对该业务日期不处于唯一 `ACTIVE`，不定义新的日期阈值或专业语义。 | 可信 USER、本人 task、task/date/risk=`OPEN/OPEN/CLEAR`、无本人 record、合法 `UPSERT_RECORD`（`expectedRecordVersion=null`）必须返回 `409 RECORD_PLAN_NOT_ACTIVE / CLEAR_ALL / []`；10 张实际迁移表完整 rows/count 前后相等。 | 产品仅冻结本单场景；研发为唯一代码写入者，只能新增 test-only PG18 测试和工程记录；QA/安全/专业/UI/运营只读，最终交指定 Sol Critical。 | 尚无本场景执行证据；不得修改 controller、builder、repository、database package、UI、生产 wiring，不得开启其他 P11-04 状态或其他 skipped。 | `施工中`：按研发 direct GREEN/RED -> QA 独立复现 -> 边界复核 -> Sol Critical -> 产品收口；任一范围扩大或无法证明零副作用立即停止。 |
| 2026-08-18 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 修复并收口 P11-04-PLAN_NOT_ACTIVE | 研发初次 direct GREEN 后，指定 Sol Critical 发现 fixture 的固定业务日期不在相对当前时间的计划有效窗口内；即使状态恢复 ACTIVE 仍可能返回同一错误，无法证明“仅非 ACTIVE”分支。 | 不接受假阳性；最小补救仅允许固定 test-only 计划窗口覆盖 `business_date=2026-01-02`，在状态变更前断言 ACTIVE 与窗口覆盖，随后唯一变更为 `SUPERSEDED`；旧 QA/边界结果作废并全部重做。 | 研发仅修改 PG18 test fixture/断言及工程记录，未改生产 controller、builder、repository、database package、UI 或生产 wiring；QA 和四边界部门对修复后版本重新独立复核。 | 修复后研发与 QA 均报告聚焦 `1 passed / 12 skipped`、PG18 `13 passed / 0 skipped`、六文件 `66 passed / 0 skipped`，typecheck/build/diff 通过；10 张表完整 rows/count 前后相等，响应为 `409 RECORD_PLAN_NOT_ACTIVE / CLEAR_ALL / []` 且无敏感回显，临时 PG18 清理并停止、5432 无监听。安全/专业/UI/运营均条件性 ALLOW；指定 Sol Critical 复审为 `GREEN / ALLOW — P11-04_PLAN_NOT_ACTIVE_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0。 | `已完成/已停止`：仅收口本 test-only 单场景；当前无下一场景授权，不证明通用计划授权、生产/真人、锁/并发/幂等/审计原子性、G2/G3、release 或 `readyForRealUsers=true`。 |
| 2026-08-18 | `[产品]` 授权 P11-05-CLIENT_USER_FIELD_REJECTED 单场景 | P11-04 已收口；依据 P11 合同“无客户端 authority 字段”，选择最小请求边界：合法 `UPSERT_RECORD` body 额外提交伪造 `userId`。 | 必须在 repository port 之前稳定返回 `400 RECORD_REQUEST_INVALID / CLEAR_ALL / []`；fake 零调用，响应不回显伪造 userId；不新增 HTTP endpoint、专业字段、数据库或生产行为。 | 产品仅冻结该一个 test-only API fake/controller 场景；研发为唯一代码写入者，QA/安全/专业/UI/运营只读，最终交指定 Sol Critical。其他 P11-05 伪造角色/时间/日期/计划/任务归属变体及其他 skipped 继续冻结。 | 尚无本场景执行证据；不得修改 production wiring、PG18 adapter、repository、database package 或 UI。 | `施工中`：按研发 RED/direct GREEN -> QA 独立复现 -> 四边界复核 -> 指定 Sol Critical -> 产品收口；任一范围扩大立即停止。 |
| 2026-08-18 | `[产品/研发/质量]` 完成 P11-05-CLIENT_USER_FIELD_REJECTED 运行复核 | 合法 USER 请求仅额外提交 forged `userId`；现有 strict command schema 在 repository port 前拒绝。 | 固定 `400 RECORD_REQUEST_INVALID / CLEAR_ALL / []`、fake 零调用、无伪造值回显；不启动 PG18。 | 研发 direct GREEN，未改生产；QA 独立复现，其他 P11-05 变体继续冻结。 | focused `1 passed / 29 skipped`；四文件 API 回归 `53 passed / 0 skipped`；typecheck/build/diff 通过，PG18 保持停止。 | `待验收`：等待安全/专业/UI/运营复核及指定 Sol Critical；产品尚未收口本场景。 |
| 2026-08-20 | `[安全/专业/UI/运营]` 完成 P11-05-CLIENT_USER_FIELD_REJECTED 边界复核 | 四部门在修复后权威目录只读核对同一 forged `userId` 单场景；未运行数据库、未修改文件。 | 安全/隐私确认 strict schema 前置拒绝、零 fake 调用和不回显；专业确认无专业字段、单位、阈值、时间、补录、风险或文案；UI 确认无改动；运营确认不接 PG18、生产/外部 DB、部署或真人。 | 四部门均为条件性 `ALLOW`，仅限 API 输入边界；其他 P11-05 变体与生产门禁继续冻结。 | 证据引用测试 `apps/api/test/p11-record-persistence.pre-wiring.e2e.spec.ts:402-413`、controller strict schema `apps/api/src/records/record-safe-structure.controller.ts:128-134`，工程记录 `docs/engineering/plans/2026-07-31-p11-get-record-context-test-only-prewiring-tdd.md:660`；研发/QA focused `1 passed / 29 skipped`、四文件 `53 passed / 0 skipped`、typecheck/build/diff 通过。 | `待验收`：仅等待指定 Sol Critical 独立复审；不得据此收口或开启其他场景。 |
| 2026-08-20 | `[产品/Sol Critical]` P11-05 独立复审线程调度失败 | 产品已向指定 Sol Critical 线程发送完整 P11-05 审查请求并重试一次；两次均返回 `429 Too Many Requests` 系统错误，未产生任何审查结论。 | 429 仅表示审查基础设施未执行；不得解释为 GREEN、BLOCK 或替代正式独立审查。 | 保持 P11-05 范围、研发/QA/四边界证据和所有冻结门禁不变；未修改代码、数据库或其他场景。 | 指定线程 `019fb206-82b9-7ae1-9b1c-1bd9136169a2` 两次返回 system error，正式审查未执行。 | `已阻塞/待重试`：待指定 Sol Critical 线程恢复后重新发送同一审查请求；产品不收口、不授权下一场景。 |
| 2026-08-20 | `[Sol Critical/产品/研发]` 闭合 P11-05 工程计划审计 BLOCK | Sol Critical 正式审查确认代码与测试行为无 Critical/Important 缺陷，但发现工程计划冻结范围、自审 spec coverage/scope check 未列已授权 P11-05，形成审计性 Important BLOCK；同时指出工作日志 controller 行号偏差。 | 必须先同步 `docs/engineering/**`：列明 forged top-level `userId` 单场景、精确 400 envelope、fake 零调用、无回显、其他 P11-05 变体冻结，并补齐 spec coverage/scope check；产品不得直接绕过。 | 研发仅修改工程计划并完成目标 diff-check/范围检查；产品校正工作日志行号；未改生产代码、数据库、UI 或产品规则。 | 工程计划现已在 `:18`、`:22`、`:660`、`:666`、`:669` 同步 P11-05 范围与证据；Sol 原 BLOCK 的审计缺口已处理，尚未重新正式审查。 | `施工中/待复审`：重新交指定 Sol Critical；GREEN 前不收口、不授权下一场景。 |
| 2026-08-20 | `[产品/Sol Critical]` 正式收口 P11-05-CLIENT_USER_FIELD_REJECTED | 产品再次向唯一指定线程 `019fb206-82b9-7ae1-9b1c-1bd9136169a2` 发送只读复审；Sol Critical 确认上一轮工程计划范围、自审清单和工作日志行号阻断均已关闭。 | 复审确认 strict schema 在 repository 前拒绝 forged top-level `userId`，精确返回 `400 RECORD_REQUEST_INVALID / CLEAR_ALL / []`、fake 零调用且不回显；证据仍仅限 API fake/controller 输入边界。 | 产品仅同步四份 `docs/product/**` 收口记录；未改代码、工程文档、数据库或 UI，未运行 PG18，未开启其他场景。 | 正式结论 `GREEN / ALLOW — P11-05_CLIENT_USER_FIELD_REJECTED_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0；沿用研发/QA focused `1 passed / 29 skipped`、四文件 `53 passed / 0 skipped`、typecheck/build/diff 证据。 | `已收口/已停止`：当前无下一场景授权；其他 P11-05 变体、其他 skipped、生产/真人/G2/G3/release 与 `readyForRealUsers=true` 继续冻结。 |
| 2026-08-21 | `[产品/研发/质量/安全/Sol Critical]` 收口 P11-08 既有记录并发更新 | 研发修复审查发现的两个 Important：唯一幂等行绑定获胜 session/HMAC digest；锁屏障、请求等待和异常清理改为有界并在 `finally` 释放。未修改生产 controller、repository、database package、migration、UI 或产品规则。 | 研发与 QA 均验证真实 PG18 `FOR UPDATE`/`pg_stat_activity.wait_event_type='Lock'`、一胜一败、最终 entries 深度相等、获胜幂等 `COMPLETED`/精确 replay、成功审计 request 绑定、失败无成功副作用和清理收敛。 | QA 独立复现返回 `QA_CLEAR`；安全/隐私复审返回 `SECURITY_NO_OBJECTION`；指定 Sol Critical 最终复审返回 `GREEN / ALLOW — P11-08_CONCURRENT_EXISTING_RECORD_UPDATE_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0。 | 本轮实际证据：focused `1 passed / 13 skipped`；PG18 文件 `14 passed / 0 skipped`；六文件 API 回归 `68 passed / 0 skipped`；typecheck/build/diff 通过；暂存区为空；临时库 `0`；PG18 已停止且 5432 无监听。 | `已完成/已停止`：仅收口 P11-08 test-only 既有记录并发更新；不授权 absent-record create、same-key replay、P11-09、其他场景、生产、真人、G2/G3、release 或 `readyForRealUsers=true`。 |
| 2026-08-23 | `[产品]` 授权 P11-09-OUT_OF_ORDER_OLD_REQUEST 单场景 | P11-08 已收口；产品二次授权唯一下一场景：较新请求先成功推进既有记录 version 1 到 version 2，随后较旧请求以 `expectedRecordVersion=1` 乱序到达。 | 仅验证服务端版本比较：旧请求返回 `409 RECORD_VERSION_CONFLICT`，最终 entries/version/schema 保持较新成功结果；失败请求无 `COMPLETED` 幂等或成功审计副作用。客户端时间不参与权威判断；不增加时间字段或专业语义。 | 研发为唯一代码与工程文档写入者，只允许现有 test-only PG18 API fixture 和测试；QA、安全/专业/UI/运营只读，最终交指定 Sol Critical。same-key replay、absent-record create、P11-10、生产/真人/G2/G3/release 继续冻结。 | `施工中`：按产品冻结 -> 研发 RED/direct GREEN -> focused PG18 -> QA -> 安全/专业/UI/运营 -> 指定 Sol Critical -> 产品收口。 |
| 2026-08-23 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-09-OUT_OF_ORDER_OLD_REQUEST | 研发新增唯一顺序场景并修正一次测试自身误把合法 `schema-v1` 当敏感值的断言；未修改生产 controller、repository、database package、migration、UI、package metadata 或产品规则。 | 研发与 QA 验证较新请求先成功推进 v1→v2，旧请求返回现有版本冲突合同，最终 entries/version/schema、获胜幂等/审计和旧请求零成功副作用均严格匹配；不引入客户端时间。 | QA `QA_CLEAR`；安全 `SECURITY_NO_OBJECTION`；专业 `PROFESSIONAL_CLEAR`；UI `UI_NO_CHANGE_CLEAR`；运营 `OPERATIONS_BLOCK_MAINTAINED`；指定 Sol Critical `GREEN / ALLOW — P11-09_OUT_OF_ORDER_OLD_REQUEST_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0。 | 本轮实际证据：focused `1 passed / 14 skipped`；PG18 文件 `15 passed / 0 skipped`；六文件 API 回归 `69 passed / 0 skipped`；typecheck/build/diff 通过；暂存区为空；临时数据库 `0`。5432 由本轮开始前外部 PID 监听，未停止非本轮进程，已如实记录为环境边界。 | `已完成/已停止`：仅收口 P11-09 test-only 顺序乱序场景；不授权 same-key replay、absent-record create、P11-10、其他 skipped、生产、真人、G2/G3、release 或 `readyForRealUsers=true`。 |
| 2026-08-23 | `[产品]` 授权 P11-10-SCHEMA_VERSION_CONFLICT 单场景 | P11-09 已收口；产品二次授权唯一下一场景：既有 schema-v1 record 收到 schema-v2 合法请求，服务端应在写入前返回 schema 版本冲突。 | 固定 `409 RECORD_SCHEMA_VERSION_CONFLICT / CLEAR_ALL / ['REFRESH']`；十张实际迁移表 rows/count 前后一致；record/version/schema/幂等/审计均不变。不增加专业字段、客户端时间或新错误语义。 | 研发为唯一代码与工程文档写入者，只允许现有 test-only PG18 API fixture；QA、安全/专业/UI/运营只读，最终交指定 Sol Critical。same-key replay、absent-record create、其他 skipped、生产/真人/G2/G3/release 继续冻结。 | `施工中`：按产品冻结 -> 研发 RED/direct GREEN -> focused PG18 -> QA -> 安全/专业/UI/运营 -> 指定 Sol Critical -> 产品收口。 |
| 2026-08-23 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-10-SCHEMA_VERSION_CONFLICT | 研发新增 schema-v2 对既有 schema-v1 record 的唯一 PG18 场景；首次失败为 fixture 断言错误，修正后 direct GREEN，未修改生产 API/database。UI 另修复现有 schema conflict 的 CLEAR_ALL 消费缺口，完成 RED→GREEN。 | API/PG18 验证 schema conflict 在幂等 claim 前 fail closed，十表零写入；UI 清空 record/draft/editor 并仅保留严格 REFRESH。 | QA `QA_CLEAR`；安全 `SECURITY_NO_OBJECTION`；专业 `PROFESSIONAL_CLEAR`；UI focused `59/59`、Web typecheck 通过；运营 `OPERATIONS_BLOCK_MAINTAINED`；指定 Sol Critical `GREEN / ALLOW — P11-10_SCHEMA_VERSION_CONFLICT_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0。 | 本轮实际证据：API focused `1 passed / 15 skipped`；PG18 `16 passed / 0 skipped`；六文件 `70 passed / 0 skipped`；API typecheck/build/diff 通过；UI `59 passed / 0 skipped`、Web typecheck 通过；暂存区为空；临时数据库 `0`。页面 `.tsx` 单独命令 `No test files found`，按 Vitest include 规则记录为未收集。5432 外部 PID 监听为本轮开始前环境状态，未停止非本轮进程。 | `已完成/已停止`：仅收口 P11-10 schema conflict；不授权 same-key replay、absent-record create、其他 skipped、生产、真人、G2/G3、release 或 `readyForRealUsers=true`。 |
| 2026-08-23 | `[产品]` 授权 P11-06-SAME_INTENT_REPLAY 单场景 | P11-10 已收口；产品二次授权唯一下一场景：同一完整规范化写入意图和同一 raw idempotency key 的两次合法请求在真实 test-only PG18 API→repository 路径中精确重放。 | 两次业务响应完全相同；record/version 只推进一次；幂等完成行和成功审计各只有一条；第二次无重复副作用。changed-intent、absent-record create、其他 skipped、专业字段和生产行为继续冻结。 | 研发为唯一代码与工程文档写入者，只允许现有 test-only PG18 fixture；QA、安全/专业/UI/运营只读，最终交指定 Sol Critical。 | `施工中`：按产品冻结 -> 研发 RED/direct GREEN -> focused PG18 -> QA -> 安全/专业/UI/运营 -> 指定 Sol Critical -> 产品收口。 |
| 2026-08-23 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口 P11-06-SAME_INTENT_REPLAY | 研发新增唯一真实 API→PG18 replay 场景，focused direct GREEN，未修改生产实现。 | 两次响应深度相同；record/version只推进一次；唯一COMPLETED幂等行、真实session/HMAC digest/精确replay_result和唯一成功审计均严格匹配；第二次零新增副作用。 | QA `QA_CLEAR`；安全 `SECURITY_NO_OBJECTION`；专业 `PROFESSIONAL_CLEAR`；UI `UI_NO_CHANGE_CLEAR`；运营 `OPERATIONS_BLOCK_MAINTAINED`；指定Sol Critical `GREEN / ALLOW — P11-06_SAME_INTENT_REPLAY_REVIEW_COMPLETE`，Critical/Important/Minor均为0。 | focused `1 passed / 16 skipped`；PG18 `17 passed / 0 skipped`；六文件 `71 passed / 0 skipped`；typecheck/build/diff通过；暂存区空；临时库0。一次错误-t过滤全skip未计证据；5432外部进程未停止。 | `已完成/已停止`：仅收口P11-06；changed-intent、absent-record create、其他skipped、生产、真人、G2/G3、release继续冻结。 |
| 2026-08-23 | `[产品]` 授权 P11-07-SAME_KEY_CHANGED_INTENT 单场景 | P11-06已收口；唯一下一场景为同一raw key改变一个opaque entry值。 | 第二次返回`409 IDEMPOTENCY_KEY_REUSED / CLEAR_ALL / ['USE_NEW_IDEMPOTENCY_KEY']`，不泄露首次结果；最终record/version/entries及唯一幂等/审计保持第一次成功结果。 | 初始研发范围仅test-only PG18测试/工程计划。UI只读复核发现client-state漏消费该既有合同后，产品在同一场景内授权UI最小修复：仅改p11-client-state及focused spec，纳入CLEAR_ALL并严格保留USE_NEW_IDEMPOTENCY_KEY；不得新增页面或产品语义。 | `施工中`：API direct GREEN、UI RED→GREEN均完成，等待工程记录同步和Sol Critical复审。 |
| 2026-08-23 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口P11-07 entry-value changed-intent | API direct GREEN；UI Important经产品补授权完成RED→GREEN，工程/产品范围漂移修复后Sol复审。 | API`1/17`、PG18`18`、六文件`72/0`；UI`60/60`；typecheck/build/diff通过，temp0/staged空。 | QA_CLEAR、SECURITY_NO_OBJECTION、PROFESSIONAL_CLEAR、OPERATIONS_BLOCK_MAINTAINED；Sol`GREEN / ALLOW — P11-07_SAME_KEY_CHANGED_INTENT_REVIEW_COMPLETE`，Critical/Important/Minor0。 | `已完成/已停止`：仅entry-value分支；其他changed-intent与absent-record create冻结。 |
| 2026-08-23 | `[产品]` 授权P11-13-AUTHORITATIVE_READ_FAILURE_AFTER_SUCCESS | P11-08 absent-create已收口；唯一下一目标为保存成功后权威GET_RECORD_CONTEXT失败时的UI stale-state处理。 | 仅UI/state：清除或禁用stale editor，不展示本地推演成功态；不新增API/数据库/专业语义。 | UI为唯一写入者，研发/QA/安全/专业/运营只读，最终交指定Sol Critical。 | `施工中`：focused UI RED→GREEN→QA→边界→Sol→产品收口。 |
| 2026-08-23 | `[产品/研发/质量/安全/专业/运营/Sol Critical]` 收口P11-13 | UI direct GREEN，权威GET失败/主体不可信清除stale状态；QA UI60/60/Web typecheck；安全/专业/运营无阻断；Sol`GREEN / ALLOW — P11-13_AUTHORITATIVE_READ_FAILURE_AFTER_SUCCESS_REVIEW_COMPLETE`，Critical/Important/Minor0。 | `已完成/已停止`：仅UI/state分支；P11-14/15/16和browser冻结。 |
| 2026-08-23 | `[产品]` 授权P11-14-UI_MALFORMED_RESPONSE_FAIL_CLOSED | P11-13已收口；唯一下一目标为未知状态/字段/动作/额外属性或畸形响应整体fail closed，不回退demo。 | 仅UI/parser/state；清除stale状态，拒绝未知合同；不新增API/database/page产品语义。 | UI唯一写入，QA/安全/专业/运营只读，Sol最终复审。 | `施工中`：UI RED→GREEN→QA→边界→Sol→产品收口。 |
| 2026-08-23 | `[产品]` 授权P11-08-CONCURRENT_ABSENT_RECORD_CREATE | 无既有record，两个不同session/key/entry请求并发创建，均expectedRecordVersion=null。 | 按repository冻结锁序在实际最早同主体account `FOR UPDATE`边界形成确定性PG18等待，随后task锁和权威状态重读；一项200/version1，一项409 RECORD_VERSION_CONFLICT/PRESERVE_DRAFT/REFRESH；最终一条胜者record、一条COMPLETED idem、一条audit。 | 研发仅test-only测试/工程计划；QA及边界部门只读，指定Sol最终复审。 | `施工中`：existing-record并发已收口，本轮仅absent-record create。 |
| 2026-08-23 | `[产品/研发/质量/安全/专业/UI/运营/Sol Critical]` 收口P11-08 absent-record create | 真实最早account锁等待、task锁/权威重读、一胜一败、胜者唯一record/idem/audit和失败零副作用均有证据；未改生产。 | QA_CLEAR；SECURITY_NO_OBJECTION；PROFESSIONAL_CLEAR；UI_NO_CHANGE_CLEAR；OPERATIONS_BLOCK_MAINTAINED；新Sol Critical `GREEN / ALLOW — P11-08_CONCURRENT_ABSENT_RECORD_CREATE_REVIEW_COMPLETE`，Critical/Important/Minor0。 | focused`1/18`、PG18`19`、六文件`73/0`、typecheck/build/diff通过、temp0、index空；外部PG PID8152未停止。 | `已完成/已停止`：仅absent-create分支；P11-13/14/15/16、双路线E2E和UI/browser证据另行冻结。 |
## 后续维护

每次新增重要问题、合并施工批次或完成独立验收后追加一行。若登记结果改变 P01-P21 的总体状态，应同步更新产品验收台账；若涉及产品范围或业务规则变化，应先更新 PRD 并完成相应审批。

| 2026-08-24 | `[产品]` 授权 P11-DUAL-FIXTURE-RECORD-WRITE 首场景 | P11-06 SAME_INTENT_REPLAY 已按既有证据收口；本目标先验证两套既有虚构 USER fixture 的合法记录写入，不把消息或数据权利未冻结合同混入。 | `persona_fat_loss` 与 `persona_muscle_gain` 各自使用可信 session、独立 opaque task/request/idempotency/entry，通过现有 record command route 到隔离 PG18；两条响应、record version、主体/task 绑定、完成幂等和成功审计分别成立，禁止跨主体泄露。 | 研发为唯一代码/工程写入者；QA、安全、专业、UI、运营只读；最终交指定 Sol Critical。禁止新增 endpoint/migration/生产 wiring/UI/消息/数据权利/专业字段/真人/G2/G3/release。 | `施工中`：按产品授权 -> RED -> 最小 GREEN -> focused PG18 -> QA/边界复核 -> Sol Critical -> 产品收口。 |
| 2026-08-24 | `[产品/研发/QA/安全/专业/UI/运营]` P11-DUAL-FIXTURE-RECORD-WRITE GREEN待Sol | named test真实隔离PG18 `1 passed / 19 skipped`；两虚构主体各200/version1，record/task、business_date、record_kind_id、COMPLETED幂等（含business_date/intent_digest）和成功审计各自绑定且公开响应无敏感回显；QA `QA_CLEAR`（仅slice）、安全 `SECURITY_NO_OBJECTION`、专业 `PROFESSIONAL_CLEAR`、UI `UI_NO_CHANGE_CLEAR`、运营 `OPERATIONS_BLOCKED / MAINTAINED`。 | 仅新增 `apps/api/test/p11-record-context.postgres.e2e.spec.ts`；工程计划同步完整意图指纹与幂等日期断言；diff-check通过、临时PG18库0；未改生产实现、migration、UI、消息或数据权利。 | UI确认本slice无 apps/web 页面/consumer/parser/client-state/browser 合同变化；运营确认维持发布阻断，不授权G2/G3/release/readyForRealUsers，且P15/P16继续独立冻结。 | `待Sol独立审查`：保持本场景停点，不收口、不授权下一场景；以上边界结论已写入本执行记录，待新Sol复审。 |
| 2026-08-24 | `[产品/研发/QA/安全/专业/UI/运营/Sol Critical]` 正式收口 P11-DUAL-FIXTURE-RECORD-WRITE | 新 Sol Critical 复审确认无 Critical/Important/Minor；正式结论 `GREEN / ALLOW — P11_DUAL_FIXTURE_RECORD_WRITE_REVIEW_COMPLETE`。 | 最终 named test `1 passed / 19 skipped`；business_date、完整 intent_digest、record/幂等/审计/authority 断言和 UI/运营边界记录均已落盘。 | 仅限 test-only、虚构双 fixture、隔离 PG18 record-write；P15/P16、生产、真人、G2/G3、release 和 readyForRealUsers 继续冻结。 | `已完成/已停止`：仅收口本 slice；下一场景另行授权。 |
| 2026-08-24 | `[产品/研发/QA/安全/专业/UI/运营/Sol Critical]` 收口 P11-DUAL-FIXTURE-RECORD-WRITE | 修复后 named test 真实隔离PG18 `1 passed / 19 skipped`；两实际 persona 映射、record business_date、完整幂等 intent_digest、审计与 authority projection 均验证。 | QA `QA_CLEAR`；安全 `SECURITY_NO_OBJECTION`；专业 `PROFESSIONAL_CLEAR`；UI `UI_NO_CHANGE_CLEAR`；运营 `OPERATIONS_BLOCKED / MAINTAINED`；新 Sol Critical 无 Critical/Important/Minor，正式结论 `GREEN / ALLOW — P11_DUAL_FIXTURE_RECORD_WRITE_REVIEW_COMPLETE`。 | 仅限本地 test-only 双虚构 fixture record-write；未修改生产、消息、P16、部署或真人路径。P15/P16、其他 skipped、G2/G3、release、readyForRealUsers 继续冻结。 | `已完成/已停止`：仅收口本 named slice；下一场景须另行产品冻结。 |
| 2026-08-24 | `[产品]` 授权 P11-DUAL-FIXTURE-MESSAGE-STATE | record-write slice 已收口；下一且仅下一场景复用既有 P15 test-only message contract fixture 与 UI parser/client/state，为两实际 persona 可信主体验证严格消息列表、已读幂等、服务端目标重读和主体变化/失效/未知/畸形输入统一 CLEAR_ALL。 | 消息对象仅四字段 `messageId/readState/opaque targetType/opaque targetId`；两主体共享同一机器语义；不新增生产消息 API、迁移、消息持久化、通知、worker、P16或专业字段。 | QA 预审 `QA_CONDITIONAL_ALLOW`；安全无阻断；运营 `OPERATIONS_BLOCKED / MAINTAINED`。研发为唯一写入者，UI/安全/专业/运营只读，最终交新 Sol Critical。 | `施工中`：按产品冻结 -> RED -> 最小 GREEN -> focused fixture/UI -> QA/边界复核 -> Sol Critical -> 产品收口。 |
| 2026-08-24 | `[产品]` 授权 P11-DUAL-FIXTURE-DATA-RIGHTS-STATE | message-state slice 已收口；下一且仅下一场景复用既有 P16 test-only EXPORT/DELETE/ANONYMIZE request-status fixture 与 UI parser/client/state，验证两实际虚构 persona 的可信主体绑定、独立 requestId、同主体幂等、跨主体 CLEAR_ALL 和未知/畸形 fail closed。 | 状态对象仅 opaque requestId/requestType/status；EXPORT 四态，DELETE/ANONYMIZE 五态含 FROZEN；不生成真实导出包、不执行删除/匿名化/留存/调度。 | 安全 `SECURITY_PREFLIGHT_CLEAR`；质量 `QA_CONDITIONAL_ALLOW`；运营 `OPERATIONS_BLOCKED / MAINTAINED`；研发为唯一写入者，最终交新 Sol Critical。 | `施工中`：按产品冻结 -> RED -> 最小 GREEN -> focused fixture/UI -> QA/隐私/专业/UI/运营复核 -> Sol Critical -> 产品收口。 |
| 2026-08-31 | `[研发/QA/安全/专业/UI/运营/Sol Critical]` P11-DUAL-FIXTURE-DATA-RIGHTS-STATE 修复与复审中 | Sol 先后发现并已修复：EXPORT/FROZEN 类型组合绕过、boxed/非字符串输入、transport raw error、request identity mismatch、reducer 未校验状态/主体/discriminator、API fixture 非法 seed/重复覆盖及证据计数同步问题。 | 当前 API fixture focused `6 passed / 0 skipped`；此前 `5 passed / 0 skipped` 仅为修复前历史基线；reducer/UI focused `20 passed`；Web 全量 `28 test files / 272 passed / 0 failed`；Web typecheck、diff-check 通过，暂存区空；严格状态、主体幂等、跨主体 `CLEAR_ALL` 和 malformed fail closed 已覆盖。 | QA `QA_CLEAR`、安全 `SECURITY_NO_OBJECTION`、专业 `PROFESSIONAL_CLEAR`、UI `UI_NO_CHANGE_CLEAR`、运营 `OPERATIONS_BLOCKED / MAINTAINED`；工程计划已同步当前 `6/20/272` 计数。 | `待Sol最终独立复审`：仅限 test-only fixture/UI state；真实导出/删除/匿名化/留存/调度、生产、真人、G2/G3、release 继续冻结。 |
| 2026-08-31 | `[产品/研发/QA/安全/专业/UI/运营/Sol Critical]` 收口 P11-DUAL-FIXTURE-DATA-RIGHTS-STATE | 新启用的 Sol Critical 独立通道确认 Critical/Important/Minor 均为 0，正式结论 `GREEN / ALLOW — P11_DUAL_FIXTURE_DATA_RIGHTS_STATE_REVIEW_COMPLETE`。 | 双 persona 各 14 个合法状态（EXPORT 4；DELETE/ANONYMIZE 各 5 含 `FROZEN`）；严格 primitive/type-specific parser、client identity/transport fail-closed、reducer runtime validation、非法 seed/duplicate guard 及同主体同 `requestId` 跨 `requestType` seed 拒绝均已记录。当前证据为 API fixture `6 passed / 0 skipped`、reducer/UI `20 passed`、Web `28 test files / 272 passed / 0 failed`；旧 `5 passed` 仅历史基线。 | QA `QA_CLEAR`、安全 `SECURITY_NO_OBJECTION`、专业 `PROFESSIONAL_CLEAR`、UI `UI_NO_CHANGE_CLEAR`、运营 `OPERATIONS_BLOCKED / MAINTAINED`；以上均仅限本命名 test-only 切片，未运行生产数据库或真实数据权利流程。 | `已完成/已停止`：仅收口本 test-only 状态 slice；真实导出/删除/匿名化/留存/调度、P15 生产消息、P11 总体双路线闭环、生产、真人、G2/G3、release 和 `readyForRealUsers=true` 继续冻结；下一场景须另行产品冻结。 |
| 2026-08-24 | `[产品/研发/QA/安全/专业/UI/运营/Sol Critical]` 收口 P11-DUAL-FIXTURE-MESSAGE-STATE | 两实际虚构 persona subject 共享严格四字段消息机器契约；各自列表隔离、已读重复幂等、服务端目标重读和跨主体/未知/失效/畸形 `CLEAR_ALL` 均通过。 | named test `1 passed / 1 skipped`；仅改 `apps/api/test/p11-message-contract.fixture.spec.ts`；QA `QA_CLEAR`（仅slice）、安全 `SECURITY_NO_OBJECTION`、专业 `PROFESSIONAL_CLEAR`、UI `UI_NO_CHANGE_CLEAR`、运营 `OPERATIONS_BLOCKED / MAINTAINED`；新 Sol Critical 无 Critical/Important/Minor，正式结论 `GREEN / ALLOW — P11_DUAL_FIXTURE_MESSAGE_STATE_REVIEW_COMPLETE`。 | 仅限 test-only fixture/UI contract 边界；不证明生产消息 API、持久化、真人深链或浏览器闭环。P16、生产、真人、G2/G3、release 和 readyForRealUsers 继续冻结。 | `已完成/已停止`：仅收口本 named slice；下一场景须另行产品冻结。 |
| 2026-08-23 | `[产品/QA/安全/专业/运营/Sol Critical]` 收口P11-13/P11-14并授权P11-15 | P11-13权威重读失败与P11-14 malformed-response均完成Sol GREEN；下一目标冻结为消息已读/安全深链。 | UI suite60/60、Web typecheck；页面tsx未收集；P11-15仅验证已读、重读、失效和越权深链fail closed。 | P11-15由UI唯一写入，QA/安全/专业/运营只读，最终交Sol。 | `施工中`：P11-15单场景流程；P11-16/browser/发布冻结。 |
| 2026-08-23 | `[产品]` 冻结P11-15最小消息/深链合同 | P11-13/14已收口；依据PRD消息中心与A15矩阵，冻结messageId、readState UNREAD/READ、opaque targetType/targetId、已读幂等、服务端重读、失效/越权CLEAR_ALL。 | 不新增专业字段、客户端授权身份、自由文案或生产行为；未知目标fail closed。 | UI/研发按合同施工，QA/安全/专业/运营只读，Sol最终复审。 | `施工中`：P11-15单场景。 |
| 2026-08-23 | `[产品]` 批准P11-15可执行API合同 | `GET /api/v1/messages`严格返回messages数组；`POST /api/v1/messages/:messageId/read`严格返回READ；`GET /api/v1/message-targets/:targetType/:targetId`服务端重读目标。 | 可信USER session、opaque定位、已读幂等、失效/越权CLEAR_ALL、未知fail closed；只做test-only fixture，不注册生产路由/迁移。 | 研发负责API contract fixture，UI负责consumer/state，QA/边界/Sol只读复核。 | `施工中`：P11-15唯一授权。 |
| 2026-08-23 | `[产品]` 批准P11-16-DATA_EXPORT_REQUEST_STATUS最小合同 | P11-15已收口；仅验证可信USER提交导出请求和查询状态。 | opaque requestId、requestType EXPORT、status SUBMITTED/PROCESSING/COMPLETED/REJECTED；同主体重复幂等，跨主体CLEAR_ALL；不生成真实导出包。 | 研发API test-only fixture，UI消费/state，QA/安全/隐私/专业/运营只读，Sol最终复审；删除/匿名化/留存和真人演练冻结。 | `施工中`：P11-16单场景。 |
| 2026-08-23 | `[产品/QA/安全/专业/运营/Sol Critical]` 收口P11-16-DATA_EXPORT_REQUEST_STATUS并授权DELETE_REQUEST_STATUS | 导出状态fixture API1/UI105/0、typecheck/build/diff/staged通过；Sol`GREEN / ALLOW — P11-16_DATA_EXPORT_REQUEST_STATUS_REVIEW_COMPLETE`，Critical/Important/Minor0。 | 仅test-only状态fixture；真实导出/删除/匿名化/留存演练冻结。下一单为DELETE_REQUEST_STATUS。 |
| 2026-08-23 | `[产品/QA/安全/专业/运营/Sol Critical]` 收口P11-16 DELETE_REQUEST_STATUS | API fixture1、UI总113/0、typecheck/build/diff/staged通过；统一EXPORT/DELETE/ANONYMIZE五态、主体幂等、跨主体CLEAR_ALL；Sol`GREEN / ALLOW — P11-16_DELETE_REQUEST_STATUS_REVIEW_COMPLETE`，Critical/Important/Minor0。 | 仅test-only状态fixture；真实删除/匿名化/留存/调度冻结。 |
| 2026-08-23 | `[产品/QA/安全/专业/运营/Sol Critical]` 收口P11-16 DELETE_REQUEST_STATUS | API fixture1、UI113/0、typecheck/build/diff/staged通过；EXPORT/DELETE/ANONYMIZE五态主体幂等/CLEAR_ALL；Sol`GREEN / ALLOW — P11-16_DELETE_REQUEST_STATUS_REVIEW_COMPLETE`，Critical/Important/Minor0。 | 仅test-only状态fixture；真实删除/匿名化/留存演练继续冻结。 |
| 2026-08-31 | `[产品]` 授权 P11-DUAL-FIXTURE-EVIDENCE-ORCHESTRATOR | 三个双路线 test-only 子切片已分别收口；当前唯一下一场景是组合既有 lifecycle、record-write、message-state、data-rights-state 证据，生成证据层分离的 G2 预备报告。 | 输出必须区分 `API_FAKE`、`PG18_REPOSITORY`、`CROSS_LAYER_E2E`、`UI_STATE`、`BROWSER`，标记实际执行/历史/未执行和缺口；缺失或冲突证据为 `INCOMPLETE`，不允许推导通过。 | 研发为唯一代码/测试写入者；QA、安全、专业、UI、运营只读；最终交新 Sol Critical。禁止新增 endpoint、迁移、生产/外部数据库、真实数据权利处理、worker/scheduler、页面/browser 行为、真人、G2/G3、release 或 `readyForRealUsers=true`。 | `施工中`：按产品冻结 -> RED -> 最小 GREEN -> focused orchestrator -> QA/边界复核 -> Sol Critical -> 产品收口；其他 skipped 继续冻结。 |
| 2026-08-31 | `[产品/研发]` 授权 P11-DUAL-FIXTURE-EVIDENCE-ORCHESTRATOR 验证性类型修复 | QA 发现 API workspace typecheck 被既有 test-only message fixture 的 `TS7006` 阻断，研发确认是 `new Map()` 泛型推断导致，非编排器运行时缺陷。 | 仅允许给既有 test-only fixture 增加最小泛型/参数类型注解以清除编译阻断；不得改变运行时行为、合同、测试范围或产品语义。 | 研发唯一写入者；修复后必须重新执行 typecheck、focused orchestrator 和完整复核；其余 skipped、生产、真人、G2/G3、release 继续冻结。 | `施工中`：类型门禁修复 -> focused 验证 -> QA/边界复核 -> 新 Sol Critical -> 产品收口。 |
| 2026-08-31 | `[产品/研发/QA/安全/专业/UI/运营/Sol Critical]` 收口 P11-DUAL-FIXTURE-EVIDENCE-ORCHESTRATOR | 三个双路线 test-only 子切片已按各自合同收口；新增 evidence-only orchestrator 以五层目录组合证据，并对缺失、冲突、未执行、boxed/跨主体和敏感 metadata fail closed。 | 主控新鲜验证 orchestrator `6 passed / 0 skipped`、message fixture `2 passed / 0 skipped`、API typecheck/build、diff-check 通过；生命周期双 persona named test 新鲜 `1 passed / 35 skipped`。PG18/browser/runtime 在编排器本轮未执行，真实预备包因此保持 `INCOMPLETE / G2_PREPARATION`。 | QA `QA_CLEAR`；安全 `SECURITY_NO_OBJECTION`；专业 `PROFESSIONAL_CLEAR`；UI `UI_NO_CHANGE_CLEAR`；运营 `OPERATIONS_BLOCKED / MAINTAINED`；新 Sol Critical `GREEN / ALLOW — P11_DUAL_FIXTURE_EVIDENCE_ORCHESTRATOR_REVIEW_COMPLETE`，Critical/Important/Minor 均为 0。Map 泛型修复仅为 test-only 类型收窄。 | `已完成/已停止`：仅收口证据编排器与 G2 预备包目录，不宣称 P11 总体双路线或 G2 通过；真实 P16、生产、真人、G2/G3、release 和 `readyForRealUsers=true` 继续冻结，下一补齐项须另行产品冻结。 |
| 2026-08-31 | `[产品]` 授权 P11-DUAL-FIXTURE-RUNTIME-BRIDGE 单场景 | G2 预备包已识别 lifecycle PGlite 上下文与 record-write 隔离 PG18 adapter 未在同一 test-only 双 persona 场景桥接。 | 仅共享测试上下文/identity bridge、goal/plan/task/session 映射及两 persona record-write 来源层验证；不得新增生产 endpoint、repository/migration、消息/P16 runtime、UI/browser、worker/scheduler、专业字段或真人路径。 | 研发为唯一 test-only builder/helper 与 named test 写入者；QA、安全、专业、UI、运营只读，最终交新 Sol Critical；任一来源不明即 `INCOMPLETE`。 | `施工中`：产品冻结 -> RED -> 最小 GREEN -> focused PG18 -> QA/边界复核 -> Sol Critical -> 产品收口。 |
| 2026-09-05 | `[产品]` 同步 P11-DUAL-FIXTURE-RUNTIME-BRIDGE 收口状态 | 依据当前状态摘要、验收台账、runtime bridge 工程计划和交接备份，确认该 test-only 双 persona API -> 隔离 PG18 bridge 已完成。 | 仅记录既有收口证据：named test `1 passed`、API typecheck/build/diff-check 通过、临时库 `0`、QA/安全/专业/UI/运营及新 Sol Critical 已复核；不将其外推为 P11 总体闭环、G2/G3、生产或真人证据。 | 仅同步工作日志，不修改代码、测试、数据库或产品范围。 | `已完成/已停止`：仅收口本 bridge；下一场景仍需单独产品授权。 |
| 2026-09-05 | `[产品]` 授权 P11-DUAL-FIXTURE-LOCAL-OPERABLE-STRUCTURE | 当前 P11 缺口是浏览器尚不能操作既有双 persona test-only API→隔离 PG18 record path；消息 P15 与数据权利 P16 不属于本场景。 | 只允许 `persona_fat_loss` 与 `persona_muscle_gain` 在本机 test-only runtime 中以各自可信 session/task 执行 schema-driven context read、一次 `UPSERT_RECORD` 和权威 reread，并验证切换主体不泄露记录。UI 只按 test-only schema 渲染 primitive 字段；不得定义饮食三态、训练逐组/补录、偏离、疼痛或其他专业语义。 | 研发可修改 test-only runtime/test support、P11 web client/page、显式 local runtime gate、启动脚本和本场景工程记录；不得改生产路由/迁移、P15/P16、真人、staging、G2/G3 或 release。 | `施工中`：RED -> 最小 GREEN -> focused API/UI -> 本机 BROWSER 实操 -> QA/边界复核 -> 新 Sol Critical；所有证据严格标注 `BROWSER`、`UI_STATE`、`CROSS_LAYER_E2E` 或 `PG18_REPOSITORY`。 |
| 2026-09-05 | `[研发/产品]` P11-DUAL-FIXTURE-LOCAL-OPERABLE-STRUCTURE 执行完成，待最终独立复核 | 本地 test-only runtime、双 fixture selector、schema-driven editor、稳定 404、防御性权威重读、`DISABLE_EDITOR` 和 cleanup 错误聚合已落地；浏览器完成两路线写入/重读/回看隔离。 | `PG18_REPOSITORY + CROSS_LAYER_E2E` focused `5/5`；`UI_STATE` client/page/selector `20/20`，加 reused client-state regression 后 `38/38`；workspace typecheck/build、生产包排除、diff-check 通过；`BROWSER` 在 1280 x 720 视口中增肌提交显示 `RECORD_CONTEXT_REFRESHED` 且记录数 1，回看减脂值为 `final-fat-browser-entry` 且无增肌值；所有 `lianban_%` 生成测试库清理后为 0。 | 默认并行 `npm test` 因 worker OOM 中止；完整单 worker 回归 `64/65 files`、`785/794 tests`，9 项失败全部来自旧 plan-lifecycle 固定日期窗口与当前可信时间冲突。该阻断不属于本命名切片，但保留为仓库级回归阻断。 | `待新 Sol Critical`：仅允许其只读复核本命名 test-only slice；P11 总体、P15/P16 生产化、G2/G3、真人、release、`readyForRealUsers=true` 继续冻结。 |
| 2026-09-05 | `[研发/Sol Critical]` P11-DUAL-FIXTURE-LOCAL-OPERABLE-STRUCTURE 首轮 REJECT 纠偏 | 首轮 Sol Critical 提出 5 个 Important：阻断草稿未回权威值、重读未绑定提交 schema、launcher cleanup 退出未传播、readiness/signal 状态机不完整、manifest locator 未去重；均限当前命名 slice。 | 新增 RED 后最小 GREEN：页面阻断回填权威 draft；taskId 与提交/响应/重读 schema、version、record/entries 严格匹配；launcher readiness timeout、shutdown escape、API cleanup exit 校验、bounded fetch 和 Windows child isolation；manifest 拒绝重复 account/task/token。client taskId response binding属于同一 fail-closed 纠偏。 | 新鲜 focused：runtime `5/5`、Web `43/43`、launcher `6/6`；API/Web typecheck/build、生产包排除、diff-check 通过；纠偏后浏览器双路线重跑通过，异常终止 Vite 后 API 关闭且 local DB 为 0；全量单 worker `65/66 files`、`794/803 tests`，仅余 9 个旧 lifecycle 日期失败。 | `待 scoped Sol Critical re-review`：其他场景和生产/真人/G2/G3/release继续冻结。 |
| 2026-09-05 | `[产品/研发/QA/安全/专业/UI/运营/Sol Critical]` 收口 P11-DUAL-FIXTURE-LOCAL-OPERABLE-STRUCTURE | scoped re-review 对首轮 5 个 Important 逐项判定 `ADDRESSED`，纠偏 diff 未发现新问题；Critical/Important/Minor 均为 0。最终只读边界为 QA `QA_CLEAR`、UI `UI_CLEAR`、本地运维 `LOCAL_SLICE_CLEAR`、安全 `SECURITY_NO_OBJECTION`、专业 `PROFESSIONAL_CLEAR`；生产运维继续 `OPERATIONS_BLOCKED`。 | 最终 fresh evidence：runtime `5/5`、Web `43/43`、launcher `6/6`；API/Web typecheck/build、生产包排除、diff-check 通过；纠偏后 `BROWSER` 双路线写入/权威重读/回看隔离通过，1280 x 720 截图留存在 `docs/engineering/evidence/`，不外推 Edge/Narrator/P19；隔离 PG18 port 5433 最终全量单 worker `65/66 files`、`796/805 tests`，仅余 9 个旧 lifecycle 固定日期失败。较早中断运行的残留库已受控清理，最终全量运行自动清理至所有 `lianban_%` 生成库为 0，PG18 停止。 | Sol 正式结论 `GREEN / ALLOW — P11_DUAL_FIXTURE_LOCAL_OPERABLE_STRUCTURE_REVIEW_COMPLETE`。该结论仅限 test-only 双虚构 persona 本地可操作结构；P11 总体、生产/staging/真人、专业语义、P15/P16 生产化、G2/G3、release、`readyForRealUsers=true` 均未关闭。 | `已完成/已停止`：本命名 slice 收口；不自动授权下一场景。 |
| 2026-09-05 | `[研发/产品]` P11-DUAL-FIXTURE-LOCAL-OPERABLE-STRUCTURE 完成审计补测 | 逐项复核实施计划时发现 Task 6 的缺失 PostgreSQL admin URL 测试未落盘；先得到真实 RED `1 failed / 6 passed`，再增加纯校验并让 launcher 在 spawn 前消费规范化 URL。 | launcher focused 更新为 `7/7`，Web+launcher focused `5 files / 50 tests`；API runtime `5/5`；workspace typecheck/build、生产包排除、diff-check 通过；带首尾空格的合法 loopback URL 实际启动两项 readiness，Ctrl+C 后端口和临时库清理为 0；隔离 PG18 全量单 worker `65/66 files`、`797/806 tests`，仍仅余 9 个旧 lifecycle 日期失败。 | 本次只补齐既有本地 launcher 配置合同，不改变前次边界与 Sol 结论；P11 总体、生产/真人/G2/G3/release 继续冻结。 | `已完成/已停止`：完成审计闭环，不自动授权下一场景。 |
| 2026-09-17 | `[产品]` 新产品部经理任务交接 | 确认 P11 本地可操作结构已进入提交 `c6dcb77` 并保持收口；当前没有活动场景。 | 新增 `docs/product/lianban-v1-handoff-2026-09-17.md`，同步当前状态入口；保留最新已执行全量证据 `65/66 files`、`797/806 tests` 与 9 项 plan-lifecycle 固定日期失败。本次未重跑测试。 | `%SystemDrive%/` 与 `SHAP_学术研究方法论_可编辑公式.docx` 为无关未跟踪内容，禁止触碰；G2/G3、生产、真人和 release 继续冻结。 | `已交接/暂停`：新任务先只读接管；是否授权生命周期固定日期回归预审由用户另行决定。 |
| 2026-09-17 | `[产品/研发]` 收口 `PLAN-LIFECYCLE-FIXED-DATE-REGRESSION-PREFLIGHT` 只读预审 | 用户授权后 ZCode 复现 `apps/api/test/plan-lifecycle.e2e.spec.ts` 为 `9 failed / 27 passed (36)`（`CROSS_LAYER_E2E`，memory:// PGlite，无需 PG18）；8 项失败均为 `preparePublished` 的 PUBLISH 得 409，1 项为多 ACTIVE bypass 读得 200。根因：受影响测试未注入 `planClock`，发布与读守卫回退 `clock_timestamp()` 真实时间；固定夹具窗口老化（失败 9 自 2026-07-29、失败 1-8 自 2026-08-08T12:00Z 起注定失败），生产代码自 2026-07-26 无改动且行为正确（`PUBLICATION_LEAD_TIME_INSUFFICIENT` 与 `resolveCurrentPlan` 过滤均 fail closed）。注入时钟的探针复跑 PUBLISH 得 200，证明 test-only 修复方向成立。 | 失败不是 2026-09-05 时间源变更所致，状态摘要原表述已精确化；2026-09-16 记录仅计数无用例名，按文件+计数+机制推断为同一批失败并如实标注。一次性探针已删除，不留仓库痕迹。 | 新增预审报告 `docs/engineering/plans/2026-09-17-plan-lifecycle-fixed-date-regression-preflight.md`；同步验收台账、状态摘要与本行。未修改任何生产代码、测试、迁移或 UI。 | 预审范围内证据完整；边界自审为 ZCode 自审（非外部独立 Sol Critical）；未运行全量回归、未启动 PG18。 | `已完成/已停止`：修复未执行；建议下一场景仅注入 `planClock`（8 项锚定 `publishAt`，bypass 项锚定 2026-07-27），test-only，须另行产品授权；G2/G3、生产、真人继续冻结。 |
| 2026-09-18 | `[产品/研发]` 执行并收口 `PLAN-LIFECYCLE-FIXED-DATE-FIXTURE-CLOCK-INJECTION`（授权于 2026-09-17） | 对九个失败 fixture 注入显式 `planClock`：发布路径 8 项锚定 `publishAt` 并新增 `trustedNow ≤ deadline − 24h` 真值断言；bypass 项锚定 2026-07-27 并新增两条窗口包含断言；未改生产代码、未改既有断言语义、未跳过或删除用例。 | 全量回归中 6 个文件 133 项 `ECONNREFUSED 127.0.0.1:5432` 系本机 PG18 服务（`postgresql-x64-18`）停止所致，属基础设施阻断而非行为回归，不得记为通过或失败回归；服务启动需管理员权限，本会话无权。 | 仅修改 `apps/api/test/plan-lifecycle.e2e.spec.ts`（十处编辑）与场景工程计划 `docs/engineering/plans/2026-09-17-plan-lifecycle-fixed-date-fixture-clock-injection.md`；同步状态摘要与本行。 | focused `1 file / 36 tests passed`（45.45s，单 worker，memory:// PGlite）；该文件在全量运行中 `36 tests ✓`；`npm run typecheck`、`npm run build` 均 `EXIT=0`；全量单 worker 复跑（PG18 启动后）`66/66 files`、`806/806 tests`、`EXIT=0`（239s），临时库 `lianban_%` 检查为 0；此前一次运行（PG18 停止）的 133 项 `ECONNREFUSED` 为基础设施阻断，非行为回归。 | `SELF_REVIEW_COMPLETE`：产品负责人 2026-09-18 接受 ZCode 自审验收（无外部独立复核线程）；全量复跑已确认 `66/66`；G2/G3、生产、真人继续冻结。 |
